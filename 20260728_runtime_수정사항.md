# OpenVLA ROS2 Runtime 노드 및 파라미터 변경 정리

## 1. 목적

본 문서는 Doosan A0509 + ZED 2i + OpenVLA 실기 구동 파이프라인에서 사용하는 각 ROS2 노드의 역할과 최근 수정 사항을 정리한다.

현재 구성의 주요 목적은 다음과 같다.

- 카메라 이미지를 OpenVLA 입력 형식으로 전달
- OpenVLA의 7차원 action 추론
- action을 두산 로봇의 절대 pose 명령으로 변환
- 두산 로봇과 디지털 그리퍼 제어
- episode 시작·종료 및 오류 관리
- 동일 이미지 반복 추론과 연속 명령 충돌 방지
- 모델 출력이 일정하게 고정되는 문제 진단

---

# 2. 전체 노드 구성

```text
ZED Camera
    │
    ▼
camera_adapter
    │  /vla/image_rgb
    ▼
openvla_inference
    │  /vla/raw_action
    ▼
action_adapter
    │  /vla/target_pose
    ▼
doosan_bridge
    │
    ├─ Doosan MoveLine
    └─ Digital gripper control

episode_manager
    ├─ /vla/instruction
    ├─ /vla/enable
    ├─ /vla/stop
    └─ episode 상태 관리
```

핵심 실행 순서는 다음과 같다.

```text
Episode 시작
→ instruction 발행
→ enable=True
→ DoosanBridge 초기 그리퍼 OPEN
→ 현재 pose 수신
→ robot_ready=True
→ OpenVLA 추론
→ raw_action 발행
→ ActionAdapter pose 변환
→ DoosanBridge 로봇 이동
→ robot_ready=False
→ 이동 완료
→ robot_ready=True
→ 다음 이미지로 다음 추론
```

---

# 3. 노드별 정리

## 3.1 camera_adapter

### 역할

ZED 또는 일반 ROS2 이미지 토픽을 구독하고 OpenVLA 추론 노드가 사용하는 `/vla/image_rgb` 토픽으로 변환한다.

### 주요 입출력

| 구분 | 토픽 |
|---|---|
| 입력 | ZED RGB 이미지 토픽 |
| 출력 | `/vla/image_rgb` |
| 메시지 | `sensor_msgs/msg/Image` |

### 기존 상태

- 카메라 이미지를 RGB 형식으로 변환
- 필요 시 resize 또는 crop 수행
- OpenVLAInferenceNode에 이미지 전달

### 변경 또는 권장 사항

1. 시작 시 파라미터와 입출력 토픽을 로그로 출력
2. 이미지 width, height, encoding 검증
3. OpenVLA processor가 자체 전처리를 수행하므로 중복 crop 주의
4. 현재는 `center_crop_square: false` 권장
5. 입력 이미지가 실제로 갱신되는지 timestamp와 hash로 확인할 수 있도록 inference 노드에서 진단

### 권장 파라미터

```yaml
camera_adapter:
  ros__parameters:
    center_crop_square: false
```

### 확인 명령

```bash
ros2 topic hz /vla/image_rgb
```

```bash
ros2 topic echo /vla/image_rgb --once
```

### 변경점 요약

| 항목 | 기존 | 변경/권장 |
|---|---|---|
| Crop | 환경에 따라 적용 | 우선 비활성화 |
| 입력 검증 | 제한적 | 크기·encoding 확인 |
| 이미지 변화 확인 | 직접 확인 어려움 | inference 로그의 image hash 사용 |

---

## 3.2 openvla_inference

### 역할

카메라 이미지와 자연어 instruction을 입력받아 OpenVLA 모델로 7차원 action을 추론한다.

기본 action 형식:

```text
[dx, dy, dz, dRx, dRy, dRz, gripper]
```

기본 단위:

```text
translation: meter
rotation: radian
gripper: 0=close, 1=open
```

### 주요 입출력

| 구분 | 토픽 | 메시지 |
|---|---|---|
| 입력 | `/vla/image_rgb` | `sensor_msgs/msg/Image` |
| 입력 | `/vla/instruction` | `std_msgs/msg/String` |
| 입력 | `/vla/enable` | `std_msgs/msg/Bool` |
| 입력 | `/vla/robot_ready` | `std_msgs/msg/Bool` |
| 출력 | `/vla/raw_action` | `std_msgs/msg/Float64MultiArray` |
| 출력 | `/vla/action_valid` | `std_msgs/msg/Bool` |
| 출력 | `/vla/model_status` | `std_msgs/msg/String` |

### 기존 동작

- `/vla/enable=True`
- `/vla/robot_ready=True`
- 최신 이미지 존재
- inference가 실행 중이 아님

위 조건을 만족하면 모델 추론을 실행했다.

### 기존 문제점

1. 추론 직전에 내부 `robot_ready=False`로 변경
2. 동일 이미지로 다시 추론할 가능성
3. 추론 실패 시 빠르게 반복 재시도 가능
4. `/vla/action_valid` 미발행
5. `unnorm_key`를 모델 시작 시 검증하지 않음
6. `repeat_action_epsilon=1e-12`로 지나치게 작음
7. episode 종료 중 실행된 이전 추론 결과가 발행될 가능성
8. action 발행 직후 로봇의 실제 busy-ready 사이클을 엄격히 기다리지 않음
9. instruction과 이미지 공유 상태의 lock 범위가 불완전
10. 체크포인트 필수 파일 검증 없음

### 수정된 핵심 내용

#### 1. 체크포인트 필수 파일 검증

다음 파일이 모두 존재해야 모델을 로드한다.

```text
config.json
model.safetensors
processor_config.json
dataset_statistics.json
```

현재 체크포인트에는 위 파일이 모두 있으므로 LoRA adapter를 별도로 로드하거나 병합할 필요가 없다.

#### 2. dataset statistics와 unnorm_key 사전 검증

모델 시작 시 `dataset_statistics.json`에 설정된 `unnorm_key`가 실제로 존재하는지 확인한다.

예:

```yaml
unnorm_key: "doosan_a0509"
```

해당 키가 없다면 첫 추론까지 기다리지 않고 노드 시작 단계에서 오류를 발생시킨다.

#### 3. 새 이미지에만 추론

내부적으로 이미지 수신마다 `latest_image_id`를 증가시키고, 마지막으로 사용한 이미지 ID와 비교한다.

```text
latest_image_id == last_used_image_id
→ 추론하지 않음
```

#### 4. 로봇 명령 사이클 핸드셰이크

한 action을 발행한 뒤 다음 상태를 기다린다.

```text
robot_ready=True
→ raw_action 발행
→ robot_ready=False 확인
→ robot_ready=True 확인
→ 다음 추론 허용
```

이를 위해 다음 상태를 사용한다.

```text
command_outstanding
robot_became_busy
```

#### 5. episode generation 적용

`/vla/enable` 상태가 바뀔 때마다 generation 값을 증가시킨다.

GPU 추론 도중 episode가 종료되거나 새 episode가 시작되면 이전 generation의 결과는 폐기한다.

#### 6. action_valid 발행

추론 성공:

```text
/vla/action_valid = True
```

추론 실패:

```text
/vla/action_valid = False
```

#### 7. 실패한 추론도 재시도 주기 제한

기존에는 성공할 때만 inference 시간이 갱신될 수 있었다.

수정 후에는 성공 여부와 관계없이 `finally`에서 마지막 시도 시간을 갱신한다.

#### 8. 반복 action 검출 허용 오차 변경

기존:

```yaml
repeat_action_epsilon: 1.0e-12
```

수정:

```yaml
repeat_action_epsilon: 1.0e-5
```

부동소수점의 미세한 차이를 무시하고 사실상 동일한 action을 감지할 수 있다.

#### 9. 이미지 진단 로그 강화

각 추론마다 다음 항목을 출력한다.

```text
action
instruction
image_id
image_stamp
image_hash
image_shape
image_min
image_max
image_mean
image_std
action_repeat_count
```

### 권장 파라미터

```yaml
openvla_inference:
  ros__parameters:
    model_path: "/home/ubuntu/robot_ws/src/openvla/runs/47a0ec7fc4ec123775a391911046cf33cf9ed83f+doosan_a0509+b8+lr-0.0005+lora-r32+dropout-0.0--handguide_10hz_resampled--image_aug"

    unnorm_key: "doosan_a0509"

    device: "cuda:0"
    use_flash_attention: true

    default_instruction: "pick up the cube"

    minimum_period_sec: 0.1
    center_crop_scale: 1.0

    do_sample: false
    temperature: 1.0
    top_p: 1.0

    log_image_hash: true
    log_image_statistics: true

    repeat_action_epsilon: 1.0e-5
    repeated_action_warn_count: 3
```

### 변경점 요약

| 항목 | 기존 | 수정 |
|---|---|---|
| 체크포인트 검사 | 없음 | 필수 4개 파일 검사 |
| unnorm_key 검사 | 첫 추론 시 간접 확인 | 시작 시 즉시 확인 |
| 동일 이미지 추론 | 가능 | image ID로 차단 |
| robot_ready 상태 | inference 노드가 내부에서 False 설정 | Bridge 상태만 사용 |
| 로봇 사이클 대기 | 부분적 | ready→busy→ready 완료 대기 |
| action_valid | 없음 | 성공/실패 발행 |
| 실패 재시도 | 빠른 반복 가능 | minimum period 적용 |
| 반복 action epsilon | `1e-12` | `1e-5` |
| 종료된 episode 결과 | 발행 가능 | generation으로 폐기 |
| 진단 로그 | hash와 action 중심 | 이미지 통계까지 추가 |

---

## 3.3 action_adapter

### 역할

OpenVLA의 상대 7차원 action을 현재 두산 로봇 pose 기준의 절대 pose로 변환한다.

입력:

```text
[dx, dy, dz, dRx, dRy, dRz, gripper]
```

출력:

```text
[x, y, z, Rx, Ry, Rz]
```

### 기존 문제점

기존 설정 예:

```yaml
translation_scale_to_mm: 30000.0
max_translation_step_mm: 1.0
```

이 설정에서는 작은 action도 지나치게 크게 스케일된 뒤 1 mm로 clipping된다.

예:

```text
모델 출력 0.01
× 30000
= 300 mm
→ max 1 mm로 clipping
```

서로 다른 출력도 모두 `+1 mm` 또는 `-1 mm`로 변환되어 로봇이 일정한 방향으로 움직이는 것처럼 보일 수 있다.

### 수정 또는 권장 사항

1. meter → millimeter 변환만 적용
2. radian → degree 변환만 적용
3. 초기 테스트에서는 rotation 적용 비활성화
4. 최대 translation step을 10 mm 수준으로 설정
5. 반복 action 거부는 디버깅 중 비활성화
6. raw, scaled, clipped action 로그 출력
7. 현재 pose가 오래된 경우 target pose 생성 차단
8. gripper threshold에 hysteresis 적용 가능

### 권장 파라미터

```yaml
action_adapter:
  ros__parameters:
    translation_scale_to_mm: 1000.0
    rotation_scale_to_deg: 57.2957795

    max_translation_step_mm: 10.0
    max_rotation_step_deg: 3.0

    apply_rotation: false

    reject_repeated_actions: false
    repeat_action_epsilon: 1.0e-5
```

### 변경점 요약

| 항목 | 기존 예시 | 수정/권장 |
|---|---|---|
| translation scale | `30000` | `1000` |
| rotation scale | 불명확 | `57.2957795` |
| max translation step | `1 mm` | `10 mm` |
| max rotation step | `1 deg` 수준 | `3 deg` |
| rotation 적용 | 환경에 따라 활성 | 초기 디버깅은 false |
| 반복 action 거부 | 사용할 수 있음 | 디버깅 중 false |
| action 로그 | 부족 | raw/scaled/clipped 출력 |

---

## 3.4 doosan_bridge

### 역할

ActionAdapter가 만든 절대 pose를 두산 로봇 MoveLine 명령으로 실행하고 디지털 출력으로 그리퍼를 제어한다.

### 기존 문제점

1. 첫 MoveLine 전에 초기 그리퍼 OPEN이 보장되지 않음
2. `motion_busy=True`일 때 들어온 target을 모두 폐기
3. 로봇 준비 전에 `robot_ready=True`가 발행될 가능성
4. pose service future가 완료되기 전에 다음 요청이 겹칠 가능성
5. emergency stop callback과 worker가 동시에 `motion_busy`를 변경할 수 있음
6. action 입력 속도와 실제 로봇 이동 속도의 차이로 명령 유실 가능

### 수정된 핵심 내용

#### 1. 초기 그리퍼 OPEN 완료 후 ready

```text
enable
→ 초기 gripper OPEN
→ 현재 pose 확보
→ robot_ready=True
→ 첫 MoveLine 허용
```

#### 2. latest target overwrite 방식

기존:

```text
motion_busy=True
→ 새 target 폐기
```

수정:

```text
motion_busy=True
→ latest pending target로 저장
→ 현재 이동 완료 후 가장 최신 target 실행
```

#### 3. ready 조건 강화

```text
enabled
AND current pose available
AND initial gripper ready
AND not motion_busy
AND not gripper_busy
```

#### 4. pose request pending

이전 pose service 요청이 완료되기 전에 새 요청을 보내지 않도록 한다.

#### 5. worker thread 단일 상태 관리

MoveLine과 gripper command를 background worker에서 순차적으로 실행한다.

#### 6. emergency stop 동작

```text
pending motion 제거
pending gripper command 제거
enable 상태 종료 또는 제어 중단
```

### 권장 파라미터

```yaml
doosan_bridge:
  ros__parameters:
    robot_namespace: "/dsr01"
    pose_poll_hz: 20.0

    linear_velocity: [20.0, 10.0]
    linear_acceleration: [40.0, 20.0]

    motion_time_sec: 0.0
    motion_timeout_sec: 10.0

    dry_run: true

    gripper_open_output_index: 1
    gripper_close_output_index: 2

    gripper_active_value: 1
    gripper_inactive_value: 0

    gripper_open_pulse_count: 2
    gripper_close_pulse_count: 1
    gripper_pulse_time_sec: 1.0
```

실제 로봇 검증 전에는 반드시 `dry_run: true`로 실행한다.

### 변경점 요약

| 항목 | 기존 | 수정 |
|---|---|---|
| 초기 gripper OPEN | 첫 이동 전 보장 안 됨 | 완료 후 ready |
| busy 중 target | 폐기 | 최신 target 보관 |
| ready 조건 | 느슨함 | pose·gripper·motion 상태 모두 확인 |
| pose polling | 요청 중첩 가능 | pending flag |
| 제어 실행 | callback 중심 | worker thread |
| emergency stop | race 가능 | pending 명령 제거 및 worker 중심 상태 관리 |

---

## 3.5 episode_manager

### 역할

OpenVLA episode 시작, instruction 발행, robot enable, 종료 조건, timeout 및 오류 처리를 담당한다.

### 주요 상태

```text
IDLE
STARTING
RUNNING
STOPPING
```

### 수정된 상태 흐름

```text
IDLE
→ /vla/set_episode=True
→ STARTING
→ instruction 발행
→ enable=True
→ /vla/robot_ready 대기
→ RUNNING
→ step 및 시간 측정
→ 종료 조건
→ STOPPING
→ enable=False
→ IDLE
```

### 수정된 핵심 내용

1. `/vla/robot_ready=True` 이후에만 RUNNING
2. episode timer도 RUNNING 진입 후 시작
3. startup timeout 적용
4. 연속 invalid action 횟수 제한
5. timeout 시 emergency stop 여부 설정
6. invalid action 누적 시 emergency stop 여부 설정
7. RUNNING이 아닐 때 들어오는 `step_done` 및 `action_valid` 무시
8. instruction publisher에 `TRANSIENT_LOCAL` QoS 적용
9. duplicate start 요청 거부
10. 정상 stop과 emergency stop 서비스 분리

### 권장 파라미터

```yaml
episode_manager:
  ros__parameters:
    instruction: "pick up the cube"

    max_steps: 30
    max_duration_sec: 60.0

    robot_ready_timeout_sec: 15.0

    invalid_action_limit: 3

    emergency_stop_on_timeout: true
    emergency_stop_on_invalid_action: true
```

### 서비스 사용

Episode 시작:

```bash
ros2 service call /vla/set_episode \
  std_srvs/srv/SetBool \
  "{data: true}"
```

정상 종료:

```bash
ros2 service call /vla/set_episode \
  std_srvs/srv/SetBool \
  "{data: false}"
```

비상 정지:

```bash
ros2 service call /vla/stop \
  std_srvs/srv/Trigger \
  "{}"
```

### 변경점 요약

| 항목 | 기존 | 수정 |
|---|---|---|
| 상태 구조 | 단순 enable 중심 | IDLE/STARTING/RUNNING/STOPPING |
| timer 시작 | enable 직후 가능 | robot_ready 이후 |
| startup timeout | 없음 또는 부족 | 적용 |
| invalid action | 기준 불명확 | 연속 횟수 제한 |
| timeout 대응 | episode 종료 중심 | emergency stop 옵션 |
| 중복 start | 가능 | 거부 |
| instruction QoS | 일반 QoS | TRANSIENT_LOCAL |

---

# 4. 주요 파라미터 변경 비교

## 4.1 OpenVLAInference

| 파라미터 | 기존 | 변경 |
|---|---:|---:|
| `minimum_period_sec` | 0.1 | 0.1 유지 |
| `center_crop_scale` | 1.0 | 1.0 유지 |
| `do_sample` | false | false 유지 |
| `repeat_action_epsilon` | `1e-12` | `1e-5` |
| `log_image_hash` | true | true 유지 |
| `log_image_statistics` | 없음 | true 추가 |
| `unnorm_key` 검증 | 없음 | 시작 시 검사 |
| 체크포인트 파일 검사 | 없음 | 시작 시 검사 |

## 4.2 ActionAdapter

| 파라미터 | 기존 예시 | 변경 권장 |
|---|---:|---:|
| `translation_scale_to_mm` | 30000 | 1000 |
| `rotation_scale_to_deg` | 불명확 | 57.2957795 |
| `max_translation_step_mm` | 1.0 | 10.0 |
| `max_rotation_step_deg` | 1.0 수준 | 3.0 |
| `apply_rotation` | 환경에 따라 true | false |
| `reject_repeated_actions` | true 가능 | false |
| `repeat_action_epsilon` | 매우 작음 | `1e-5` |

## 4.3 DoosanBridge

| 파라미터 | 권장값 |
|---|---|
| `pose_poll_hz` | 20.0 |
| `linear_velocity` | `[20.0, 10.0]` |
| `linear_acceleration` | `[40.0, 20.0]` |
| `motion_timeout_sec` | 10.0 |
| `dry_run` | 초기에는 true |
| `gripper_pulse_time_sec` | 1.0 |

## 4.4 EpisodeManager

| 파라미터 | 권장값 |
|---|---:|
| `max_steps` | 30 |
| `max_duration_sec` | 60.0 |
| `robot_ready_timeout_sec` | 15.0 |
| `invalid_action_limit` | 3 |
| `emergency_stop_on_timeout` | true |
| `emergency_stop_on_invalid_action` | true |

---

# 5. 고정 action 문제 진단 기준

OpenVLAInference 로그에서 다음 항목을 확인한다.

```text
image_hash
action
action_repeat_count
image_mean
image_std
```

## 경우 1: image hash가 계속 동일

가능한 원인:

- camera_adapter 입력 정지
- rosbag에 동일 프레임 반복
- 잘못된 이미지 토픽 구독
- timestamp는 변하지만 실제 pixel 데이터가 동일

## 경우 2: image hash는 변경되지만 action이 동일

가능한 원인:

- fine-tuned model collapse
- action label 분포 편향
- 학습과 배포 prompt 불일치
- 학습과 배포 preprocessing 불일치
- 실제 카메라 구도와 학습 데이터의 domain gap
- `dataset_statistics.json` normalization 문제
- ActionAdapter clipping 포화

## 경우 3: raw action은 다르지만 target pose가 비슷함

가능한 원인:

- `translation_scale_to_mm` 과도
- `max_translation_step_mm` 과도하게 작음
- 모든 action이 clipping 경계에 포화
- rotation 비활성화 또는 pose update 문제

## 경우 4: raw action과 target pose는 정상인데 로봇 경로가 동일

가능한 원인:

- DoosanBridge가 busy 중 명령을 폐기
- 현재 pose 갱신이 느리거나 stale
- MoveLine 서비스가 이전 target만 실행
- robot_ready timing 오류
- target queue 처리 문제

---

# 6. 개별 노드 테스트 순서

## 6.1 Bag 기반 모델 출력 테스트

실행 대상:

```text
camera_adapter
openvla_inference
```

실행하지 않을 대상:

```text
action_adapter
doosan_bridge
episode_manager
```

### CameraAdapter 실행

```bash
ros2 run <package_name> camera_adapter_node \
  --ros-args \
  --params-file <config_file>
```

### OpenVLAInference 실행

```bash
ros2 run <package_name> openvla_inference_node \
  --ros-args \
  --params-file <config_file>
```

### instruction 및 enable

```bash
ros2 topic pub --once \
  /vla/instruction \
  std_msgs/msg/String \
  "{data: 'pick up the cube'}"
```

```bash
ros2 topic pub --once \
  /vla/enable \
  std_msgs/msg/Bool \
  "{data: true}"
```

### robot_ready 테스트 입력

첫 추론 허용:

```bash
ros2 topic pub --once \
  /vla/robot_ready \
  std_msgs/msg/Bool \
  "{data: true}"
```

다음 추론 허용:

```bash
ros2 topic pub --once \
  /vla/robot_ready \
  std_msgs/msg/Bool \
  "{data: false}"

sleep 0.2

ros2 topic pub --once \
  /vla/robot_ready \
  std_msgs/msg/Bool \
  "{data: true}"
```

### Bag 재생

```bash
ros2 bag play <bag_directory>
```

### 출력 확인

```bash
ros2 topic echo /vla/raw_action
```

```bash
ros2 topic echo /vla/action_valid
```

```bash
ros2 topic echo /vla/model_status
```

---

# 7. 실제 로봇 연결 전 검증 단계

## 단계 1. 모델 출력 검증

```text
camera_adapter
openvla_inference
```

## 단계 2. ActionAdapter 연결

```text
camera_adapter
openvla_inference
action_adapter
```

## 단계 3. DoosanBridge dry-run

```yaml
dry_run: true
```

## 단계 4. 실제 로봇 저속 구동

```yaml
dry_run: false
linear_velocity: [10.0, 5.0]
linear_acceleration: [20.0, 10.0]
max_translation_step_mm: 5.0
apply_rotation: false
```

---

# 8. 권장 통합 YAML 예시

```yaml
camera_adapter:
  ros__parameters:
    center_crop_square: false

openvla_inference:
  ros__parameters:
    model_path: "/home/ubuntu/robot_ws/src/openvla/runs/47a0ec7fc4ec123775a391911046cf33cf9ed83f+doosan_a0509+b8+lr-0.0005+lora-r32+dropout-0.0--handguide_10hz_resampled--image_aug"
    unnorm_key: "doosan_a0509"

    device: "cuda:0"
    use_flash_attention: true

    default_instruction: "pick up the cube"

    minimum_period_sec: 0.1
    center_crop_scale: 1.0

    do_sample: false
    temperature: 1.0
    top_p: 1.0

    log_image_hash: true
    log_image_statistics: true

    repeat_action_epsilon: 1.0e-5
    repeated_action_warn_count: 3

action_adapter:
  ros__parameters:
    translation_scale_to_mm: 1000.0
    rotation_scale_to_deg: 57.2957795

    max_translation_step_mm: 10.0
    max_rotation_step_deg: 3.0

    apply_rotation: false

    reject_repeated_actions: false
    repeat_action_epsilon: 1.0e-5

doosan_bridge:
  ros__parameters:
    robot_namespace: "/dsr01"
    pose_poll_hz: 20.0

    linear_velocity: [20.0, 10.0]
    linear_acceleration: [40.0, 20.0]

    motion_time_sec: 0.0
    motion_timeout_sec: 10.0

    dry_run: true

    gripper_open_output_index: 1
    gripper_close_output_index: 2

    gripper_active_value: 1
    gripper_inactive_value: 0

    gripper_open_pulse_count: 2
    gripper_close_pulse_count: 1
    gripper_pulse_time_sec: 1.0

episode_manager:
  ros__parameters:
    instruction: "pick up the cube"

    max_steps: 30
    max_duration_sec: 60.0

    robot_ready_timeout_sec: 15.0

    invalid_action_limit: 3

    emergency_stop_on_timeout: true
    emergency_stop_on_invalid_action: true
```

> 주의: 위 YAML의 `doosan_bridge` 키 앞에 오타가 없어야 한다. 실제 파일에서는 반드시 `doosan_bridge:`로 작성한다.

---

# 9. 최종 핵심 변경 사항

1. OpenVLAInference는 새 이미지에만 추론한다.
2. action 발행 후 실제 로봇의 busy-ready 완료를 기다린다.
3. 추론 성공과 실패를 `/vla/action_valid`로 전달한다.
4. `unnorm_key`와 checkpoint 파일을 시작 시 검증한다.
5. 고정 action 진단을 위해 image hash와 이미지 통계를 기록한다.
6. ActionAdapter의 translation scale을 `30000 → 1000`으로 변경한다.
7. ActionAdapter의 step clipping을 `1 mm → 10 mm`로 완화한다.
8. 초기 테스트에서는 rotation과 repeated-action rejection을 비활성화한다.
9. DoosanBridge는 초기 gripper OPEN과 pose 확보 후에만 ready가 된다.
10. DoosanBridge는 busy 중 최신 target을 보관한다.
11. EpisodeManager는 robot_ready 이후에만 RUNNING과 timer를 시작한다.
12. 실제 로봇 연결 전 반드시 `dry_run: true`로 전체 흐름을 검증한다.

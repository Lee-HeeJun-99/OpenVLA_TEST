근거리 action이 너무 작아서 실행 단계에서 사라짐
학습 데이터에서 근거리 상태의 action이 0에 가까움
action clipping이 너무 강함

1. 큐브 근처 데이터에 정지 또는 작은 action이 과다함
2. 모델의 작은 action이 adapter/robot에서 무시됨
3. 근거리·파지 전환 데이터가 부족함
4. 학습과 추론 이미지 구도 또는 전처리가 다름
5. 새로운 Doosan embodiment 자체의 차이
6. 224×224 해상도 자체의 한계

ros2 topic pub --once /vla/enable \
  std_msgs/msg/Bool "{data: true}"

ros2 topic pub --once /vla/instruction \
  std_msgs/msg/String "{data: 'pick up the cube'}"

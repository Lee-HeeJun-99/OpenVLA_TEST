# A0509 VLA Sim-to-Real 연구 상세 설명서

작성일: 2026-09-17 KST  
기준 프로젝트: `/home/ubuntu/a0509_vla_linux_field_bundle_20260903`  
상세 작업 루트: `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj`  
주요 보고서: `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/실험 보고서.md`

이 문서는 처음 보는 사람이 A0509 VLA Sim-to-Real 연구를 이해할 수 있도록, 패키지 구성, 데이터 흐름, phase별 실험 목적과 순서, 분석 방법, 결과, 결론을 자세히 정리한 문서다.

## 0. 한 줄 요약

이 연구는 Real과 Sim 이미지가 다르다는 사실 자체를 보이려는 것이 아니다.

핵심은 다음 질문이다.

```text
Real과 Sim의 차이 중에서
VLA policy가 실제 action을 예측할 때 중요한 차이는 무엇인가?
```

즉, 단순히 이미지나 representation이 비슷해지는 것이 목표가 아니라, 최종 policy action이 비슷해지는 것이 목표다.

가장 중요한 결론은 다음이다.

```text
Representation Gap이 크다고 해서 반드시 Action Gap이 큰 것은 아니다.
Representation Gap을 줄였다고 해서 반드시 policy가 좋아지는 것도 아니다.
```

현재 실험에서는 작은 policy-sensitive component를 맞추는 것이 전체 representation을 크게 맞추는 것보다 Action Gap 감소에 더 중요했다.

## 1. 전체 연구 배경

### 1.1 문제 상황

로봇 manipulation policy를 Sim에서 학습하거나 검증한 뒤 Real robot에 적용하려면 Sim-to-Real gap이 문제가 된다.

일반적으로는 다음과 같은 차이가 생긴다.

- Real camera와 Sim camera의 화각 차이
- 조명, 그림자, 색감 차이
- 테이블, 배경, 로봇 외형 차이
- object 위치, 크기, pose 차이
- gripper 상태와 motion timing 차이
- real robot control과 sim replay control 차이
- policy preprocessing 차이

하지만 모든 차이가 policy 성능에 같은 정도로 중요한 것은 아니다.

예를 들어 이미지 밝기가 많이 달라도 policy action에는 거의 영향이 없을 수 있다. 반대로 이미지 차이는 작아 보여도 policy가 중요하게 보는 object 위치나 gripper 주변 feature가 달라지면 action이 크게 바뀔 수 있다.

### 1.2 이 연구의 핵심 관점

이 연구는 다음 chain을 따라간다.

```text
Environment Gap
    ↓
Observation Gap
    ↓
Representation Gap
    ↓
Policy-Relevant Representation Gap
    ↓
Action Gap
    ↓
Real Performance
```

현재까지는 Real Performance, 즉 실제 real robot success까지는 가지 않았다. 본 문서의 결과는 offline action-level evidence다.

다시 말해:

```text
Real robot task success improved
```

는 아직 주장하지 않는다.

현재 주장 가능한 것은:

```text
기존 225-pair offline dataset에서
Real/Sim observation 차이가 policy 내부 representation과 action prediction에 어떻게 전달되는지 분석했고,
일부 policy-sensitive component를 줄이면 offline Action Gap이 크게 줄었다.
```

까지다.

## 2. 프로젝트 전체 폴더와 패키지 설명

메인 프로젝트:

```text
/home/ubuntu/a0509_vla_linux_field_bundle_20260903
```

이 폴더는 이번 연구의 기준 bundle이다. Real/Sim 분석, VLA feature extraction, Isaac Sim replay, OFT runtime, output, 검증 기록이 모두 이 bundle을 중심으로 정리되어 있다.

### 2.1 주요 외부 경로

| 경로 | 역할 |
|---|---|
| `/home/ubuntu/a0509_vla_linux_field_bundle_20260903` | 메인 연구 bundle |
| `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806` | Isaac Sim base package |
| `/home/ubuntu/Downloads/isaac-sim-standalone-5.1.0-linux-x86_64` | Isaac Sim standalone 실행 환경 |
| `/home/ubuntu/robot_ws/src/doosan-robot2` | Real Doosan robot 관련 ROS package |
| `/home/ubuntu/robot_ws/src/doosan-robot2/raw_dataset_oft_200` | 기존 real dataset |
| `/home/ubuntu/robot_ws/src/openvla_doosan_runtime` | Real robot에서 OpenVLA/OFT policy 실행하는 runtime |

### 2.2 메인 bundle 내부 구성

#### `environment/`

```text
/home/ubuntu/a0509_vla_linux_field_bundle_20260903/environment/a6000_ubuntu22_py310
```

Python 가상환경이다. 대부분의 offline feature extraction, analysis script, plotting script가 이 환경의 Python으로 실행되었다.

주요 역할:

- VLA feature extraction
- hidden/action analysis
- CSV/JSON summary 생성
- Phase 6 full-forward summarization

#### `models/`

```text
models/base_openvla_7b
models/oft_mixed480_step28560
models/vanilla_s1_balanced_step8130
```

모델 weight 또는 관련 모델 파일이 저장된 위치다.

이번 연구의 주 분석 대상은 `oft_mixed480_step28560` 계열이며, 실제 merged runtime checkpoint는 `runtime_state/oft_mixed480_step28560_merged`에 있다.

#### `runtime_state/`

중요 경로:

```text
runtime_state/oft_mixed480_step28560_merged
```

이번 offline 분석에서 사용한 policy checkpoint다.

정확한 policy:

```text
variant: oftplus_h5_vision
use_proprio: false
checkpoint step: 28560
action chunk size: 5
instruction: Pick up the orange cube.
```

이 checkpoint는 현재 ROS runtime에서 쓰는 `oftplus_h5_proprio` checkpoint와 다르다.

#### `runtime/openvla-oft/`

OFT/OpenVLA runtime code가 있는 위치다.

이번 feature extraction provenance:

```text
extract_vla_features.py
    ↓
serve_a0509_oft.py / OFTRuntime
    ↓
runtime_state/oft_mixed480_step28560_merged
    ↓
oftplus_h5_vision
```

즉, feature/action 분석은 이 runtime path를 사용해 생성되었다.

#### `sim2real_analysis/`

Sim-to-Real 분석 스크립트들이 들어 있다.

대표 하위 폴더:

| 폴더 | 역할 |
|---|---|
| `00_config` | 분석 configuration |
| `04_features` | VLA/OFT feature extraction |
| `05_offline_actions` | offline action extraction/분석 |
| `10_analysis` | image distribution, representation metric 등 분석 |
| `common` | 공통 sample/manifest 처리 |

이번 연구에서 중요한 script:

```text
sim2real_analysis/04_features/extract_vla_features.py
```

Real/Sim image를 VLA/OFT에 넣고 다음 feature를 저장한다.

```text
vision_backbone.input
vision_backbone.output
projector.input
projector.output
action_hidden_states.input
action_head.output
```

#### `isaac_sim/`

Isaac Sim scene/replay 관련 코드가 들어 있다.

주요 역할:

- Sim scene loading
- robot home pose 확인
- real trajectory 기반 sim replay
- sim image 수집
- cube/table/camera condition 확인

초기 작업에서는 Real trajectory를 Sim에서 replay하면서 이미지 수집을 맞추는 작업이 진행되었다.

#### `outputs/`

기존 분석 결과가 저장된 위치다.

중요 경로:

```text
outputs/token_distribution_analysis/5_episodes
```

5개의 Real/Sim paired episode에 대한 feature/action analysis baseline이 저장되어 있다.

주요 내용:

- paired manifest
- Real/Sim feature manifest
- 225 paired frames
- vision/projector/action-hidden/action-head features
- observation/representation/action metric

#### `data/real_world/`

메인 bundle 안으로 옮긴 Real world data가 저장되는 위치다.

실험 중 real data collection 경로를 bundle 내부로 옮겼고, 새 episode collection은 이쪽을 기준으로 하도록 변경했다.

#### `lhj/`

이번 상세 연구 작업의 중심 폴더다.

주요 phase 결과가 여기에 정리되어 있다.

```text
lhj/
  phase1_audit/
  phase1_observation_token_gap/
  phase1_action_gap/
  phase2_full_validation/
  phase3_oft_variant_audit/
  phase4_policy_relevance/
  phase5_policy_relevant_completion/
  phase6_environment_attribution/
  scripts/
  작업기록.md
  실험 보고서.md
  자세하게.md
```

이번 문서가 바로:

```text
lhj/자세하게.md
```

이다.

### 2.3 Real robot side package

#### `/home/ubuntu/robot_ws/src/doosan-robot2`

Doosan robot ROS package다.

이번 연구에서 사용되거나 수정된 주요 파일:

```text
dsr_example2/dsr_example/dsr_example/simple/align.py
dsr_example2/dsr_example/dsr_example/simple/collect_episode001_from_align_home.py
dsr_example2/dsr_example/dsr_example/simple/yellow_blue_position.py
dsr_example2/dsr_example/dsr_example/simple/trace_6x6_grid_corners.py
```

역할:

- Real robot home pose 설정
- orange cube pick trajectory 수집
- block 위치 이동 helper
- 6x6 grid corner tracing
- Real image collection

초기 연구에서는 Real trajectory를 기준으로 Sim replay를 맞추기 위해, real collection script와 sim replay script의 coordinate, home pose, gripper timing, cube 위치 등을 맞추는 작업이 있었다.

#### `/home/ubuntu/robot_ws/src/openvla_doosan_runtime`

Real robot에서 VLA/OFT policy를 돌리는 runtime package다.

중요한 점:

현재 ROS deployment는 `oftplus_h5_proprio` 계열을 사용한다.

하지만 이번 offline 225-pair 분석은 `oftplus_h5_vision`을 사용했다.

따라서:

```text
이번 offline 결과 = current ROS deployment 결과
```

라고 해석하면 안 된다.

## 3. Vision / Proprio policy 관계

중간에 반드시 검증한 중요한 사항이 있다.

처음에는 `oftplus_h5_vision`과 `oftplus_h5_proprio`가 완전히 다른 모델인지, 같은 모델의 옵션 차이인지 불명확했다.

코드/config/checkpoint audit 결과:

```text
둘은 완전히 별개의 architecture가 아니다.
같은 base OpenVLA/OFT architecture를 사용한다.
proprio variant에서는 7D proprio branch가 추가된다.
```

관계:

```text
Image
 ↓
Vision Backbone
 ↓
Projector
 ↓
Multimodal / Action-facing Representation
 ↓
Action Head
 ↓
Action Chunk
```

Proprio variant에서는 추가로:

```text
7D proprio state
 ↓
normalization
 ↓
proprio projector
 ↓
4096D representation
 ↓
1 proprio token
 ↓
vision patch tokens와 결합
```

하지만 중요한 차이:

| 항목 | Offline 연구 | Current ROS |
|---|---|---|
| variant | `oftplus_h5_vision` | `oftplus_h5_proprio` |
| use_proprio | false | true |
| checkpoint | step 28560 | step 6000 |
| input | image only | image + 7D proprio |
| preprocessing | offline OFT extraction path | ROS runtime config |
| instruction | `Pick up the orange cube.` | runtime config 기준 |

따라서 본 연구의 결과는 `oftplus_h5_vision` offline 연구 결과로 고정한다.

## 4. 데이터 흐름

전체 데이터 흐름은 다음과 같다.

```text
Real episode images
        ↓
paired_manifest.json
        ↓
Sim replay images
        ↓
same image encoder / same policy
        ↓
feature extraction
        ↓
vision_backbone.output
projector.output
action_hidden_states.input
action_head.output
        ↓
Representation Gap / Action Gap 계산
        ↓
correction / perturbation / ablation
        ↓
final action gap 비교
```

사용한 paired data:

```text
episode_000001: 46 pairs
episode_000002: 47 pairs
episode_000003: 44 pairs
episode_000004: 45 pairs
episode_000005: 43 pairs
total: 225 pairs
```

검증된 데이터 조건:

- Real feature manifest: 225 records
- Sim feature manifest: 225 records
- missing feature: 0
- missing image: 0
- duplicate frame: 0
- image size: `1280x720`
- image mode: RGB
- instruction: `Pick up the orange cube.`
- variant: `oftplus_h5_vision`
- action chunk size: 5

## 5. 실험 진행 순서 전체 요약

전체 연구는 대략 다음 순서로 진행되었다.

```text
Phase 1
기존 dataset과 sim replay, policy provenance audit

Phase 2
기존 결과 재현성 검증 및 offline validation

Phase 3
oftplus_h5_vision / oftplus_h5_proprio 관계 검증

Phase 4
Policy-Relevant Representation Gap 분석

Phase 5
Correction robustness, held-out validation, low-rank sensitive subspace

Phase 6
Environment / preprocessing factor attribution
```

각 phase는 앞 phase의 결과를 무조건 믿지 않고, raw file, manifest, config, script를 확인하면서 진행했다.

## 6. Phase 1: Audit

### 6.1 목적

기존 결과를 그대로 믿지 않고, 어떤 데이터와 코드가 실제로 사용되었는지 확인하는 단계다.

확인 대상:

- Real episode 수와 frame 수
- Sim replay episode 수와 frame 수
- paired manifest integrity
- Real/Sim image correspondence
- replay 방식
- robot home pose
- feature extraction provenance
- policy checkpoint
- 현재 ROS deployment와 offline bundle 분석의 차이

### 6.2 주요 확인 결과

재사용 가능한 baseline:

```text
5 Real episodes
5 Sim replay episodes
225 paired frames
bundle OFT oftplus_h5_vision
instruction: Pick up the orange cube.
```

paired count:

```text
episode_000001: 46
episode_000002: 47
episode_000003: 44
episode_000004: 45
episode_000005: 43
```

Sim replay:

- Real source steps의 joint trajectory를 사용했다.
- Sim HOME으로 수렴한 뒤 replay했다.
- command recorded joint position을 기준으로 matching했다.
- Sim target vs actual joint error는 매우 작았다.

중요하게 남은 제한:

- Real pose는 measured feedback가 아니라 planned/commanded pose 기반이다.
- Real TCP와 Sim EEF frame은 단순 numeric 비교하면 안 된다.
- Real camera/geometry는 calibration 완료 상태가 아니다.

### 6.3 Phase 1 결론

기존 5-episode paired dataset은 offline method development 용도로 재사용 가능하다.

하지만 다음 claim은 금지했다.

```text
이 결과가 current ROS real deployment에 그대로 적용된다.
camera/lighting/geometry가 원인이라고 확정할 수 있다.
unseen layout에 generalization된다고 말할 수 있다.
```

관련 산출물:

- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase1_audit/phase1_audit_completion_summary.md`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase1_audit/pair_correspondence_summary.json`

## 7. Phase 2: Offline Validation

### 7.1 목적

Phase 1에서 재사용하기로 한 기존 결과가 재현 가능한지 확인했다.

검증 대상:

- manifest integrity
- rerun observation/token gap
- rerun LOO representation shift
- rerun policy action shift
- rerun representation-action correlation

### 7.2 주요 결과

결과 비교에서 주요 rerun은 모두 PASS였다.

예:

```text
rerun_observation_token_gap: PASS
rerun_loo_representation_shift: PASS
rerun_policy_action_shift: PASS
rerun_repr_action_gap: PASS
```

대표 observation gap:

```text
brightness_abs_diff_mean: 61.0193
rgb_mean_l2_mean: 106.1935
resized_psnr_mean: 10.6124
resized_luma_ssim_global_mean: 0.5888
```

대표 policy-relevant shift:

```text
hidden_shift_chunk_mean_l2:
  raw:       0.370262
  corrected: 0.072325
```

### 7.3 Phase 2 결론

기존 offline 결과는 현재 bundle 기준으로 재현 가능했다.

하지만 deployment equivalence는 INVALID / RE-RUN REQUIRED다.

이유:

- current ROS runtime은 proprio variant를 사용한다.
- checkpoint가 다르다.
- preprocessing flag가 다르다.
- instruction/runtime adapter가 다를 수 있다.

관련 산출물:

- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase2_full_validation/comprehensive_offline_validation_summary.json`

## 8. Phase 3: OFT Variant Audit

### 8.1 목적

`oftplus_h5_vision`과 `oftplus_h5_proprio`의 관계를 추측이 아니라 실제 코드/config/checkpoint 기준으로 확인했다.

### 8.2 확인 결과

판정:

```text
same base OFT architecture + optional proprio branch/token
```

proprio branch:

```text
7D proprio state
    ↓
ProprioProjector
    ↓
4096D proprio token
    ↓
vision patch tokens 뒤에 append
```

공통점:

- 같은 base model class
- vision backbone common parameter shape 동일
- action head class/shape 동일
- action chunk size 5

차이:

- checkpoint 다름
- proprio projector 존재 여부 다름
- input interface 다름
- preprocessing/runtime config 다름

### 8.3 Phase 3 결론

다음 표현은 틀렸다.

```text
vision과 proprio는 완전히 다른 모델이다.
```

대신 이렇게 표현해야 한다.

```text
동일한 base OFT architecture의 서로 다른 input/configuration variant이며,
현재 비교 대상 checkpoint도 서로 다르다.
```

본 연구의 225-pair evidence는 `oftplus_h5_vision`에 한정한다.

관련 산출물:

- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase3_oft_variant_audit/oft_vision_proprio_audit_summary.json`

## 9. Phase 4: Policy-Relevant Representation Gap 분석

### 9.1 목적

Phase 4부터 연구의 핵심으로 들어간다.

질문:

```text
Real/Sim representation difference 중
어떤 성분이 실제 action prediction을 바꾸는가?
```

분석 layer:

```text
action_hidden_states.input
```

이 layer는 action head 바로 직전 representation이므로, policy action과 가장 직접적으로 연결된다.

### 9.2 기본 개념

각 paired frame에서:

```text
h_real = Real observation에서 나온 action_hidden_states.input
h_sim  = Sim observation에서 나온 action_hidden_states.input
Δh_RS  = h_sim - h_real
```

policy action head:

```text
a = f(h)
```

단순히 `||Δh_RS||`를 보는 것이 아니라:

```text
f(h_real + δ) - f(h_real)
```

즉 hidden perturbation이 action을 얼마나 바꾸는지 직접 확인했다.

### 9.3 주요 실험

#### Real->Sim direction perturbation

Real hidden에서 Sim hidden 방향으로 이동시켰을 때 action이 어떻게 바뀌는지 확인했다.

#### Random / orthogonal control

같은 norm의 random direction, orthogonal direction과 비교했다.

목적:

```text
Real->Sim direction이 단순히 norm이 커서 중요한가?
아니면 policy-sensitive direction을 포함해서 중요한가?
```

#### Sensitive / null decomposition

Real->Sim difference를 개념적으로 다음처럼 나눴다.

```text
Δh_RS = Δh_sensitive + Δh_null
```

결과:

| Method | Action Gap | Representation Reduction |
|---|---:|---:|
| no correction | 0.370245 | 0.000000 |
| sensitive projection | 0.136015 | 0.009250 |
| null residual | 0.274003 | 0.884700 |

해석:

- sensitive projection은 representation을 1%도 줄이지 않았지만 Action Gap을 크게 줄였다.
- null residual은 representation을 크게 줄였지만 Action Gap 개선은 약했다.

이것이 `Representation Gap magnitude != Policy relevance`의 핵심 evidence다.

#### Token sensitivity

token별로 action sensitivity를 분석했다.

결과:

- top-k token correction은 효과가 있었다.
- 하지만 top10만으로 충분하지 않았고, top20 이상에서 효과가 커졌다.
- action effect는 특정 token 몇 개에만 몰려 있지 않고 diffuse했다.

### 9.4 Phase 4 결론

Action Gap을 설명하는 핵심은 전체 representation distance가 아니라 action-sensitive direction/component다.

관련 산출물:

- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase4_policy_relevance/policy_relevant_gap_synthesis.md`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase4_policy_relevance/action_hidden_sensitive_null_decomposition/sensitive_null_decomposition_summary.json`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase4_policy_relevance/action_hidden_token_sensitivity/token_sensitivity_summary.json`

## 10. Phase 5: Correction Robustness / Held-Out Validation

### 10.1 목적

Phase 4에서 action-sensitive component가 중요하다는 것을 확인했다.

Phase 5에서는 다음을 검증했다.

```text
correction 효과가 random/control보다 좋은가?
held-out episode에서도 유지되는가?
over-correction은 어떻게 줄일 수 있는가?
작은 low-rank sensitive subspace가 Action Gap을 설명하는가?
```

### 10.2 Correction robustness

LOO episode split으로 correction을 비교했다.

| Correction | Action Gap |
|---|---:|
| No correction | 0.370245 |
| Global mean | 0.125657 |
| Progress | 0.072325 |
| Phase | 0.066682 |
| Progress + Phase | 0.061829 |
| Random matched-norm | 0.367535 |
| Shuffled progress | 0.130975 |
| Wrong direction | 0.485313 |

해석:

- progress/phase structure가 중요하다.
- random norm matching은 거의 효과가 없다.
- wrong direction은 policy action을 더 망친다.

### 10.3 Gate generalization

correction은 평균적으로 좋지만 일부 frame을 악화시킬 수 있다.

그래서 gate를 걸어 correction을 적용할 frame을 제한했다.

| Gate | Action Gap | Worsened Frames |
|---|---:|---:|
| progress_all | 0.072325 | 33 |
| phase_gate_train | 0.072086 | 14 |
| progress_gate_lambda | 0.073059 | 2 |
| progress_gate_zero_worse | 0.081698 | 1 |

해석:

- gate를 걸면 평균 gap은 조금 희생하더라도 worsened frame을 크게 줄일 수 있다.
- deployment 관점에서는 무조건 correction보다 gated correction이 더 안전할 수 있다.

### 10.4 Low-rank sensitive subspace

P0 train fold에서 local action-sensitive direction들을 모아 low-rank basis를 만들었다.

결과:

```text
low_rank_sensitive_basis, k=128
Action Gap: 0.006676
Representation reduction: 0.030669
```

매우 작은 representation reduction으로 Action Gap을 거의 없애는 결과가 나왔다.

하지만 이 방법은 paired Sim hidden delta를 사용하므로 그대로 runtime 적용 가능하다고 주장할 수 없다.

### 10.5 Phase 5 결론

Phase 5는 다음을 강하게 지지한다.

```text
policy-sensitive hidden component를 직접 겨냥하면
전체 representation을 맞추지 않아도
Action Gap을 크게 줄일 수 있다.
```

관련 산출물:

- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase5_policy_relevant_completion/final_phase5_summary.md`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase5_policy_relevant_completion/final_evidence_matrix.csv`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase5_policy_relevant_completion/01_correction_robustness/loo_correction_ablation_summary.json`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase5_policy_relevant_completion/02_gate_generalization/loo_gate_generalization_summary.json`

## 11. Phase 6: Environment / Observation Attribution

### 11.1 목적

Phase 4/5에서는 hidden representation 내부에서 무엇이 action에 중요한지 보았다.

Phase 6에서는 질문을 한 단계 앞으로 옮겼다.

```text
어떤 observation/environment factor가
policy-sensitive representation component를 만들거나 줄이는가?
```

실험 chain:

```text
Environment / preprocessing factor
        ↓
Observation change
        ↓
Vision representation change
        ↓
Projector representation change
        ↓
Action-hidden policy-sensitive change
        ↓
Final Action Gap change
```

중요:

Phase 6에서는 이미지 metric만 보지 않고, 모든 주요 condition에 대해 full-forward feature/action extraction을 수행했다.

### 11.2 Preprocessing ablation

조건:

| Condition | 의미 |
|---|---|
| P0 | current paired image baseline |
| P1 | direct resize 224 |
| P2 | center crop square + resize |
| P3 | 0.875 center crop + resize |
| P4 | letterbox 224 |
| P5 | brightness match |
| P6 | contrast match |

Baseline:

```text
P0 Action Gap: 0.370203
```

가장 좋은 결과:

```text
P4_letterbox_224
Action Gap: 0.101993
Action Gap reduction: 72.45%
Hidden Gap reduction: 32.08%
Vision Gap reduction: 23.42%
Gripper Gap: 0.315337 -> 0.120110
```

해석:

- 단순 resize인 P1은 거의 효과가 없다.
- crop/letterbox처럼 image framing을 바꾸는 preprocessing이 큰 효과를 냈다.
- Action Gap의 큰 부분은 gripper component에서 왔다.

### 11.3 Photometric ablation

Photometric condition:

```text
P5_brightness_match_sim_to_real
P6_contrast_match_sim_to_real
```

결과:

```text
P5 brightness:
  Observation MSE: 0.026690
  Action Gap: 0.376782
  Action change: -1.78%

P6 contrast:
  Action Gap: 0.371546
  Action change: -0.36%
```

중요한 점:

P5는 observation MSE를 가장 좋게 만들었지만 Action Gap은 오히려 악화되었다.

따라서:

```text
image statistic alignment != policy alignment
```

### 11.4 Camera image-space sensitivity

Camera 실험은 실제 camera calibration이 아니라 image-space perturbation이다.

조건:

```text
C0 original
C1 sim shift left 24px
C2 sim shift right 24px
C3 sim shift up 24px
C4 sim shift down 24px
C5 zoom in 1.05
C6 zoom out 0.95
C7 rotate ccw 2deg
C8 rotate cw 2deg
```

Best condition:

```text
C2_sim_shift_right_24px
Action Gap: 0.335682
Action reduction: 9.33%
Hidden Gap reduction: 2.81%
Vision Gap reduction: -1.06%
```

중요 대조:

```text
C1 shift left:
  Observation MSE improves
  Action Gap worsens

C2 shift right:
  Observation MSE worsens
  Action Gap improves
```

Policy-sensitive energy:

```text
C0 sensitive energy: 67.585359
C2 sensitive energy: 62.328616
C2 sensitive reduction: 7.78%
```

해석:

- camera-like perturbation에서도 observation metric보다 policy-sensitive energy가 action 변화와 더 잘 맞았다.
- 하지만 C2 효과는 P4 preprocessing 효과보다 훨씬 작다.

### 11.5 Combined P4 + C2

P4와 C2가 둘 다 좋았으니 합치면 더 좋아질까?

이를 확인하기 위해 combined condition을 만들었다.

결과:

```text
K0_P4_letterbox:
  Action Gap: 0.101274
  Hidden Gap: 187.747536
  Sensitive Energy: 29.077619

K1_P4_letterbox_C2_shift_right_24px:
  Action Gap: 0.130148
  Hidden Gap: 200.902644
  Sensitive Energy: 37.908585
```

P4 위에 C2를 추가하면:

```text
Action Gap: 28.51% 악화
Hidden Gap: 7.01% 악화
Sensitive Energy: 30.37% 악화
```

결론:

좋은 single factor를 합친다고 항상 좋아지는 것이 아니다.

환경 factor는 condition-dependent하고 non-additive하다.

### 11.6 Environment alignment x hidden correction

P4 preprocessing과 hidden correction이 상호 보완적인지 확인했다.

2x2 형태:

```text
Original P0 raw
Original P0 + hidden correction
P4 raw
P4 + hidden correction
```

결과:

```text
P0 raw:                 0.370203
P0 + correction:        0.061464

P4 raw:                 0.101993
P4 + P4 correction:     0.072575
P4 + P0 correction:     0.090531
```

해석:

- P4는 raw 상태에서 이미 gap을 크게 줄인다.
- P4-specific hidden correction은 추가 개선을 만든다.
- P0-trained correction을 P4에 transfer하면 개선이 약하다.
- correction도 environment/preprocessing condition에 따라 calibration이 다르다.

### 11.7 Held-out episode condition selection

같은 데이터에서 best condition을 고른 과적합 가능성을 줄이기 위해 LOO condition selection을 했다.

결과:

```text
Train action-min:
  P4 selected 5/5 folds
  held-out action gap: 0.102457

Train hidden-min:
  P4 selected 5/5 folds
  held-out action gap: 0.102457

Train observation-MSE-min:
  P5 selected 5/5 folds
  held-out action gap: 0.378114
```

결론:

- action/hidden 기준으로 고르면 P4가 안정적으로 선택된다.
- observation MSE 기준으로 고르면 P5 brightness가 선택되지만 action은 악화된다.

### 11.8 Phase 6 결론

Phase 6의 핵심 결론:

```text
Observation metric을 줄이는 factor와
Action Gap을 줄이는 factor는 다르다.
```

현재 가장 강한 offline preprocessing factor:

```text
P4_letterbox_224
```

하지만 P4를 바로 deployment recommendation으로 해석하면 안 된다. 실제 ROS preprocessing/runtime과 policy semantics를 다시 확인해야 한다.

관련 산출물:

- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/final_phase6_summary.md`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/final_evidence_matrix.csv`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/environment_factor_table.csv`

## 12. 전체 실험 순서 정리

처음부터 따라가면 다음 순서다.

### Step 1. Real/Sim 기본 조건 맞추기

초기에는 Real과 Sim의 home pose, joint, camera, cube 위치, table 높이, gripper timing을 맞추는 작업이 있었다.

목적:

```text
Real trajectory를 Sim에서 replay하고
동일한 기준으로 image pair를 만들기 위해서
```

이때 중요한 결정:

- Sim HOME 확인 후 trajectory replay
- Real trajectory는 home-relative joint delta 기반
- Orange cube pick trajectory 중심
- Real/Sim image 수집 경로를 bundle 내부로 통일

### Step 2. 5 episode dataset 구성

여러 orange cube 위치에서 Real/Sim episode를 수집했다.

최종 분석에 사용된 paired dataset:

```text
5 episodes
225 pairs
```

### Step 3. 동일 policy로 feature extraction

Real/Sim image를 동일한 `oftplus_h5_vision` policy에 넣었다.

저장 feature:

```text
vision_backbone.output
projector.output
action_hidden_states.input
action_head.output
```

### Step 4. Observation / Representation Gap 측정

기본적으로 Real/Sim image와 representation이 다름을 확인했다.

하지만 이 단계는 최종 목적이 아니었다.

### Step 5. Action Gap 측정

동일 policy에서 Real observation action과 Sim observation action이 얼마나 다른지 측정했다.

Baseline:

```text
Action Gap ≈ 0.370245
```

### Step 6. Policy-relevant component 찾기

action_hidden_states.input에서 Real->Sim direction, random direction, sensitive/null direction을 비교했다.

결론:

```text
작은 sensitive component가 action을 크게 바꾼다.
```

### Step 7. Correction robustness 확인

Progress/phase correction과 random/wrong-direction control을 비교했다.

결론:

```text
structured correction이 control보다 훨씬 좋다.
```

### Step 8. Environment factor attribution

Preprocessing, brightness, contrast, camera shift, combined condition을 하나씩 바꿔가며 full-forward로 Action Gap까지 확인했다.

결론:

```text
P4 letterbox가 가장 강한 offline factor.
Photometric matching은 action을 개선하지 않음.
Camera effect는 작고 condition-dependent.
```

## 13. 분석 방법을 쉽게 설명

### 13.1 Gap이란 무엇인가?

Gap은 Real과 Sim이 얼마나 다른지를 뜻한다.

예:

```text
Observation Gap:
  Real image와 Sim image의 차이

Representation Gap:
  model 내부 feature 차이

Action Gap:
  최종 action prediction 차이
```

일반적으로는 representation gap이 작으면 좋다고 생각할 수 있다. 하지만 이 연구에서 확인한 것은 그렇게 단순하지 않다는 점이다.

### 13.2 왜 Action Gap이 중요한가?

robot을 움직이는 것은 representation이 아니라 action이다.

따라서 궁극적으로 중요한 것은:

```text
Real observation을 넣었을 때 policy가 내는 action이
Sim observation에서 기대한 action과 얼마나 다른가?
```

이다.

### 13.3 왜 action_hidden_states.input을 봤는가?

`action_hidden_states.input`은 action head 바로 직전의 hidden representation이다.

즉:

```text
vision/projector/VLM 처리 이후
action head가 실제 action을 만들기 위해 받는 입력
```

이다.

그래서 policy-relevant gap을 찾기에 가장 직접적인 layer다.

### 13.4 Sensitive / Null이란 무엇인가?

Real hidden과 Sim hidden의 차이 `Δh`가 있다고 하자.

이 차이 중 일부는 action을 크게 바꾸고, 일부는 action을 거의 안 바꿀 수 있다.

```text
Δh = action-sensitive component + action-null component
```

이 연구에서는 action-sensitive component를 줄이면 representation 전체를 별로 줄이지 않아도 Action Gap이 크게 줄어드는 것을 확인했다.

### 13.5 왜 observation MSE만 보면 안 되는가?

P5 brightness는 Real/Sim image pixel 차이를 크게 줄였다.

하지만 action은 나빠졌다.

즉:

```text
image가 pixel metric으로 더 비슷해졌다
≠
policy가 더 비슷한 action을 낸다
```

policy가 중요하게 보는 정보가 object 위치, gripper 주변 geometry, crop/framing에 있다면 brightness보다 preprocessing geometry가 훨씬 중요할 수 있다.

## 14. 주요 숫자 모음

### Dataset

```text
episodes: 5
pairs: 225
image size: 1280x720
policy: oftplus_h5_vision
checkpoint: step 28560
action chunk: 5x7
```

### Baseline

```text
Raw Action Gap: 0.370245
```

### Phase 4 / 5 correction

```text
Progress correction:        0.072325
Phase correction:           0.066682
Progress + Phase:           0.061829
Low-rank sensitive basis:   0.006676
Wrong direction:            0.485313
```

### Phase 6 preprocessing

```text
P0 baseline:                0.370203
P4 letterbox:               0.101993
P2 center crop square:      59.70% reduction
P3 center crop 0.875:       51.24% reduction
P5 brightness:              -1.78% worsened
P6 contrast:                -0.36% worsened
```

### Camera sensitivity

```text
C2 shift right:             0.335682
C2 reduction:               9.33%
C1 shift left:              0.382553
C1 worsened:                -3.34%
```

### Combined

```text
K0 P4:                      0.101274
K1 P4+C2:                   0.130148
K1 worsened vs K0:          28.51%
```

## 15. 최종 결론

### 결론 1. Real/Sim representation gap은 존재한다.

Real image와 Sim image를 같은 policy에 넣으면 vision/projector/action-hidden representation이 다르게 나온다.

하지만 이것만으로는 연구 기여가 충분하지 않다.

### 결론 2. 모든 representation gap이 policy에 중요한 것은 아니다.

Sensitive/null decomposition 결과, representation을 크게 줄여도 action은 별로 좋아지지 않을 수 있었다.

반대로 representation을 조금만 줄여도 action은 크게 좋아질 수 있었다.

### 결론 3. Policy-sensitive component를 보는 것이 중요하다.

Action-hidden layer의 작은 sensitive component가 Action Gap을 크게 설명했다.

### 결론 4. Observation-level alignment만으로는 부족하다.

Brightness matching은 observation MSE를 줄였지만 action은 나빠졌다.

따라서 image similarity metric만 기준으로 Sim/Real alignment를 선택하면 잘못된 결론을 낼 수 있다.

### 결론 5. Preprocessing/framing이 현재 가장 큰 영향 요인이다.

현재 offline dataset에서는 P4 letterbox가 Action Gap을 가장 크게 줄였다.

하지만 이것을 바로 real deployment recommendation으로 사용하면 안 된다. 실제 ROS runtime preprocessing과 policy checkpoint가 다르기 때문이다.

### 결론 6. 좋은 factor들이 항상 합쳐져서 더 좋아지는 것은 아니다.

C2 shift는 original image에서는 약간 좋았지만 P4와 결합하면 악화되었다.

따라서 environment factor는 condition-dependent하며, combination은 반드시 full-forward로 다시 검증해야 한다.

## 16. 현재 주장 가능한 것과 불가능한 것

### 주장 가능한 것

```text
기존 225-pair offline oftplus_h5_vision dataset에서
policy-sensitive representation component가 Action Gap과 강하게 연결되어 있다.
```

```text
Progress/phase hidden correction과 P4 preprocessing은
offline Action Gap을 크게 줄였다.
```

```text
Observation metric 개선이 반드시 Action Gap 개선을 의미하지 않는다.
```

### 아직 주장하면 안 되는 것

```text
Real robot task success가 개선되었다.
```

```text
현재 ROS proprio deployment에서도 같은 결과가 나온다.
```

```text
camera/lighting/geometry가 Real failure의 원인이다.
```

```text
P4 letterbox를 바로 실제 robot runtime에 적용하면 성공률이 오른다.
```

```text
5 episode 결과가 unseen layout까지 일반화된다.
```

## 17. 다음 단계

### 17.1 ROS/proprio deployment track

현재 real runtime은 `oftplus_h5_proprio`를 사용한다.

따라서 deployment 검증을 하려면 별도 분석이 필요하다.

필요한 것:

```text
Real/Sim synchronized proprio
proprio normalization
proprio-policy feature extraction
proprio-policy action extraction
current ROS preprocessing audit
runtime action adapter 확인
```

### 17.2 Runtime preprocessing audit

P4가 offline에서 좋았지만, 실제 runtime에 적용하려면 다음을 확인해야 한다.

```text
현재 ROS image preprocessing
training image preprocess와의 차이
P4 적용 시 tensor distribution
policy action 변화
safety boundary
```

### 17.3 추가 layout / trajectory dataset

현재 5 episodes는 충분히 큰 dataset이 아니다.

다음이 필요하다.

```text
다양한 orange cube 위치
다양한 distractor 배치
unseen trajectory
unseen layout held-out test
```

### 17.4 Shadow mode

Real robot에 policy action을 바로 연결하지 말고 먼저 log-only shadow mode를 권장한다.

```text
Real robot은 reference trajectory 수행
VLA는 live image를 보고 action prediction만 logging
robot command에는 연결하지 않음
reference action과 predicted action 비교
```

이후 안전성이 확인되면 real closed-loop test를 별도 승인 후 진행한다.

## 18. 산출물 인덱스

### 18.1 종합 보고서

- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/실험 보고서.md`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/자세하게.md`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/작업기록.md`

### 18.2 Phase 1 / 2 / 3

- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase1_audit/phase1_audit_completion_summary.md`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase1_audit/pair_correspondence_summary.json`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase2_full_validation/comprehensive_offline_validation_summary.json`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase3_oft_variant_audit/oft_vision_proprio_audit_summary.json`

### 18.3 Phase 4 / 5

- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase4_policy_relevance/policy_relevant_gap_synthesis.md`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase5_policy_relevant_completion/final_phase5_summary.md`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase5_policy_relevant_completion/final_evidence_matrix.csv`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase5_policy_relevant_completion/01_correction_robustness/loo_correction_ablation_summary.json`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase5_policy_relevant_completion/02_gate_generalization/loo_gate_generalization_summary.json`

### 18.4 Phase 6 CSV / JSON

- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/final_phase6_summary.md`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/final_evidence_matrix.csv`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/environment_factor_table.csv`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/preprocessing_ablation.csv`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/photometric_ablation.csv`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/camera_ablation.csv`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/combined_ablation.csv`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/physical_x_hidden_correction.csv`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/heldout_generalization.csv`

### 18.5 Phase 6 Figures

- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/figures/action_gap_by_condition.png`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/figures/gripper_gap_by_condition.png`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/figures/observation_mse_by_condition.png`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/figures/hidden_gap_by_condition.png`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/figures/vision_gap_reduction_vs_action_gap_reduction.png`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/figures/hidden_gap_reduction_vs_action_gap_reduction.png`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/figures/camera_action_gap_by_condition.png`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/figures/camera_sensitive_energy_vs_action_reduction.png`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/figures/combined_action_gap.png`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/figures/combined_sensitive_energy.png`
- `/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/phase6_environment_attribution/figures/environment_x_hidden_correction_2x2.png`

## 19. 이 문서를 읽는 순서 추천

처음 보는 사람은 다음 순서로 읽으면 된다.

1. 이 문서의 `0~5장`: 연구 목적, 패키지, 데이터 흐름 이해
2. `6~8장`: audit과 policy scope 이해
3. `9~11장`: 실제 핵심 실험 이해
4. `13~15장`: 분석 방법과 결론 이해
5. `18장`: 필요한 raw result file 확인

더 짧은 요약이 필요하면:

```text
/home/ubuntu/a0509_vla_linux_field_bundle_20260903/lhj/실험 보고서.md
```

를 먼저 읽으면 된다.

## 20. 최종 메시지

이번 연구에서 가장 중요한 메시지는 다음이다.

```text
Sim과 Real을 무조건 시각적으로 비슷하게 만드는 것이 목표가 아니다.

중요한 것은 policy가 action을 결정할 때 실제로 사용하는
representation component를 맞추는 것이다.
```

현재 offline 결과는 이 방향을 강하게 지지한다.

다만 실제 Real robot 성능 개선을 주장하려면, current ROS proprio runtime을 별도 track으로 검증하고 shadow mode 및 안전 검증을 거쳐야 한다.

현재 가지고 있는 VLA 패키지를 기준으로 Sim-to-Real 분석 실험을 진행하려고 한다.

중요한 점은 바로 코드를 수정하지 말고, **먼저 현재 패키지 전체 구조와 기존 데이터/추론/로봇 제어 파이프라인을 분석한 뒤 작업 계획을 제시하는 것**이다.

# 1. 현재 패키지 전체 점검

먼저 프로젝트 루트부터 전체 구조를 확인하라.

다음 항목을 찾아서 정리하라.

* OpenVLA Vanilla 관련 코드
* OpenVLA-OFT 관련 코드
* 모델 checkpoint 및 adapter 위치
* inference server/client 구조
* 이미지 preprocessing 코드
* Vision Encoder 관련 코드
* projector / multimodal projection 관련 코드
* action decoder / action head 관련 코드
* action normalization / denormalization 코드
* proprioception/state 입력 구조
* gripper 입력 및 출력 처리
* action chunking 관련 코드
* OFT의 K 값 및 실제 실행 방식
* robot command 전송 코드
* control frequency 설정
* camera 입력 코드
* simulation data loader
* real robot data loader
* 기존 validation 코드
* logging 코드
* emergency stop / hold / timeout 처리 코드

각 항목에 대해 다음 형식으로 정리하라.

1. 파일 경로
2. 주요 class/function
3. 입력
4. 출력
5. 현재 역할
6. Sim2Real 실험에서 재사용 가능한지
7. 수정이 필요한지

패키지에 이미 구현되어 있는 기능을 새로 중복 구현하지 말 것.

---

# 2. 현재 모델의 정확한 입력/출력 파악

OpenVLA Vanilla와 OFT 각각에 대해 실제 코드 기준으로 다음을 확인하라.

## Vision input

* 입력 camera 개수
* wrist / external camera 여부
* 입력 image resolution
* crop
* resize
* RGB/BGR
* normalization
* image processor
* 학습 시 preprocessing과 inference 시 preprocessing이 동일한지

## Robot state input

* joint position
* joint velocity
* EEF pose
* gripper
* proprioception dimension
* normalization 방식

실제로 모델에 어떤 state가 들어가는지 정확히 확인하라.

## Action output

각 모델이 출력하는 action representation을 정확히 확인하라.

예:

* joint delta
* Cartesian delta
* absolute pose
* rotation representation
* gripper
* action dimension

OpenVLA Vanilla와 OFT의 차이도 정리하라.

---

# 3. OFT Action Chunking 확인

OFT의 action chunk 구조를 실제 코드에서 확인하라.

현재 예상은 다음과 같다.

* inference frequency: 약 1 Hz
* robot control frequency: 5 Hz
* action chunk K=5

즉 한 번 추론해서

[a_t, a_t+1, a_t+2, a_t+3, a_t+4]

5개의 action을 생성하고 0.2초 간격으로 실행하는 구조인지 확인하라.

반드시 코드 기준으로 확인하고 추측하지 말 것.

다음도 확인하라.

* K가 실제 어디서 설정되는가
* inference마다 chunk를 전부 사용하는가
* 일부 action만 사용하는가
* 새로운 inference와 이전 chunk가 overlap되는가
* action buffer가 존재하는가
* timeout 시 기존 action을 계속 사용하는가
* stale action 방지 로직이 있는가

---

# 4. Gripper 처리 확인

특히 OFT의 gripper output을 확인하라.

Real robot gripper가 binary actuation인데 모델 output이 continuous라면 후처리가 필요할 수 있다.

현재 코드에서 다음을 확인하라.

* gripper raw output 범위
* open / close 값 정의
* threshold 존재 여부
* hysteresis 존재 여부
* 실제 digital IO mapping
* Sim과 Real에서 open/close 정의가 반대인지
* gripper actuation delay 처리 여부

필요하면 별도의 gripper post-processing module 설계를 제안하라.

단, 기존 코드가 있으면 우선 재사용하라.

---

# 5. 이번 Sim2Real 실험의 목적

이번 실험의 핵심은 단순 Real 성공률 비교가 아니다.

다음을 순서대로 분석하는 것이 목적이다.

Sim Image
→ Vision Encoder
→ Visual Representation
→ VLA Action

Real Image
→ Vision Encoder
→ Visual Representation
→ VLA Action

그리고 각 단계에서 Sim과 Real의 distribution 차이를 확인한다.

핵심 질문은 다음과 같다.

1. Sim과 Real의 image observation이 얼마나 다른가?
2. 그 차이가 Vision Encoder representation에서도 유지되는가?
3. projector 이후 visual representation에서도 차이가 유지되는가?
4. 최종 VLA action distribution까지 차이가 전달되는가?
5. image alignment를 수행하면 feature/action gap도 감소하는가?

---

# 6. 우선 구현할 실험: Offline Sim-Real Analysis

Real robot을 바로 제어하지 않고 기존 Sim dataset과 Real dataset을 사용하여 offline 분석부터 수행한다.

단, 기존 Real 데이터의 trajectory는 Sim과 task 수행 방식이 다를 수 있다.

현재 기존 Real trajectory는:

* 카메라를 이용해 먼저 정렬
* 정렬 후 수직으로 하강
* cube grasp

방식이다.

반면 Sim trajectory는:

* 별도의 alignment phase 없이
* cube 방향으로 직접 접근
* grasp

방식이다.

따라서 기존 Real trajectory의 action을 Sim VLA의 ground truth처럼 직접 비교하면 안 된다.

기존 Real 데이터는 우선 다음 용도로 사용한다.

* image distribution 분석
* Vision Encoder feature 분석
* visual representation 분석

Action/trajectory reference 비교는 Sim과 Real의 trajectory generation 방식이 동일한 데이터가 확보된 이후 수행한다.

---

# 7. Image Distribution 분석 코드 추가

Sim dataset과 Real dataset의 실제 모델 입력 이미지를 수집하는 분석 코드를 작성하라.

가능하면 학습/추론 코드에서 사용 중인 기존 image preprocessing 함수를 그대로 사용해야 한다.

Raw image와 model input image를 둘 다 저장할 것.

각 image에 대해 최소한 다음 통계를 계산하라.

* resolution
* RGB channel mean
* RGB channel std
* brightness
* contrast
* min/max
* optional histogram

단순 pixel distribution 외에 가능한 경우 다음 scene geometry 정보도 분석 가능하도록 구조를 열어둘 것.

* cube center pixel
* cube width/height
* gripper center
* gripper apparent size

객체 detection이나 segmentation 모델을 새로 도입하지 말고, 현재 annotation이나 known cube location으로 가능한 경우에만 사용하라.

---

# 8. Vision Encoder Feature 추출

OpenVLA Vanilla와 OFT 각각에 대해 Vision Encoder 중간 feature를 추출할 수 있는 hook 또는 feature extraction module을 추가하라.

가능하면 다음 위치를 지원하라.

* encoder early layer
* encoder middle layer
* encoder final layer
* projector input
* projector output / actual visual representation entering multimodal model

모델 architecture를 먼저 확인한 후 실제 가능한 위치를 선정하라.

임의의 layer name을 가정하지 말 것.

각 frame에 대해 feature 전체를 무조건 저장하면 데이터가 너무 커질 수 있으므로 다음 두 가지 모드를 지원하라.

1. full feature 저장
2. pooled feature 저장

pooled feature는 token/patch dimension에 대해 mean pooling 등을 사용하는 방식으로 만들 수 있으나, architecture에 적절한 pooling 방식을 먼저 확인하라.

저장 형식 예:

.npy 또는 .npz

metadata와 sample ID를 반드시 연결할 것.

---

# 9. Sim-Real Feature Distribution 비교

추출된 feature를 이용해서 다음을 계산하는 분석 스크립트를 작성하라.

기본 metric:

* cosine similarity / cosine distance
* L2 distance
* mean/std
* PCA

추가적으로 필요하면:

* MMD
* Wasserstein distance

를 지원하라.

UMAP은 필수 dependency가 아니라 optional로 처리하라.

중요:

distribution visualization만 하지 말고 numerical metric을 함께 출력할 것.

예:

layer, cosine_distance_mean, cosine_distance_std, MMD, sample_count

---

# 10. Paired Sim-Real 비교 지원

가능하면 동일한 physical layout의 Sim과 Real sample을 pair로 연결할 수 있는 구조를 만들어라.

metadata 기준 예:

layout_id
cube_x
cube_y
cube_z
robot_initial_joint
eef_pose
gripper_state
instruction

동일 layout끼리는 다음을 계산할 수 있도록 한다.

D_input
D_encoder
D_projector
D_action

단, 현재 기존 데이터에 정확한 paired scene이 없다면 억지로 matching하지 말 것.

pair가 없는 경우에는 population-level distribution comparison만 수행하고, 결과에 unpaired comparison임을 명시할 것.

---

# 11. VLA Action Distribution 추출

각 Sim/Real observation을 OpenVLA Vanilla와 OFT에 넣고 action을 추론하되 robot으로 command를 보내지 않는 offline inference mode를 구현하라.

반드시 다음을 저장하라.

* model name
* checkpoint
* sample ID
* instruction
* raw image path
* processed image path
* raw robot state
* normalized model state
* raw model output
* denormalized action
* gripper raw output
* gripper postprocessed output
* inference latency

OFT는 반드시 full action chunk도 저장하라.

예:

action_chunk shape = [K, action_dim]

---

# 12. Action Distribution 분석

Sim input에서의 모델 출력과 Real input에서의 모델 출력을 비교한다.

즉:

p(a_VLA | Sim observation)

vs

p(a_VLA | Real observation)

을 비교한다.

각 action dimension별로:

* mean
* std
* min
* max
* histogram
* percentile
* distribution distance

를 분석한다.

Action representation이 translation + rotation + gripper라면 각 부분을 분리해서 결과를 출력하라.

OFT는 K별 분석도 지원한다.

예:

K1
K2
K3
K4
K5

각 chunk index에서 Sim/Real action distribution이 얼마나 다른지 분석할 수 있도록 한다.

---

# 13. 중요한 해석 원칙

Action distribution이 다르다고 바로 모델 실패라고 판단하면 안 된다.

먼저 다음 관계를 분석한다.

Image Gap
→ Encoder Gap
→ Projected Representation Gap
→ Action Gap

즉 image domain shift가 모델 내부에서 어떻게 전달되는지를 확인하는 것이 핵심이다.

결과 분석에서는 예를 들어 다음과 같은 관계를 볼 수 있도록 데이터를 구성하라.

camera/image correction 전:

D_image ↑
D_encoder ↑
D_action ↑

camera/image correction 후:

D_image ↓
D_encoder ↓
D_action ↓

이런 관계를 추후 검증할 수 있도록 코드 구조를 만들어라.

---

# 14. Camera Alignment 데이터 비교 기능

Sim과 Real camera 설정을 비교할 수 있는 별도 utility를 작성하거나 기존 기능을 찾아 재사용하라.

확인 항목:

* intrinsic matrix
* focal length
* principal point
* FoV
* distortion coefficient
* image resolution
* crop
* resize
* camera extrinsic
* camera-to-EEF transform
* camera orientation

가능하면 config 파일 두 개를 입력하면 차이를 출력하도록 한다.

Real camera undistortion이 필요한 경우에도 기존 calibration parameter를 먼저 찾고, 없으면 새 calibration이 필요하다고 명시하라.

임의로 distortion 값을 만들지 말 것.

---

# 15. 이후 단계: Robot Control Matching

Offline distribution 분석 이후 Real robot control을 진행하기 전에 별도 calibration이 필요하다.

현재 Real robot의 이동 속도가 Sim보다 약 3배 빠른 것으로 보인다.

AI 없이 동일 command를 Sim과 Real에 각각 넣어서 다음을 비교할 수 있는 테스트 코드/로그 구조를 점검하라.

* commanded action
* actual joint displacement
* actual EEF displacement
* velocity
* acceleration
* execution time
* command interval

목표는 단순 속도 숫자를 같게 만드는 것이 아니라 동일 action에 대해 가능한 한 유사한 state transition을 만드는 것이다.

p(s_{t+1} | s_t, a_t)_sim

vs

p(s_{t+1} | s_t, a_t)_real

---

# 16. Safety 기능 점검

Real 제어 전에 기존 코드에 다음 기능이 있는지 확인하라.

* inference timeout
* communication timeout
* stale action protection
* NaN / Inf action protection
* action range check
* workspace limit
* joint limit
* HOLD
* emergency stop
* recovery/home sequence

없으면 추후 추가가 필요하다고 정리하라.

지금 단계에서는 기존 코드 구조를 확인한 후 안전하게 추가 가능한 위치를 제안하라.

---

# 17. Logging 설계

향후 Shadow Mode와 Real rollout에서 매 timestep 다음 데이터가 저장되어야 한다.

timestamp

camera:

* raw image
* processed image

robot:

* raw joint position
* raw joint velocity
* EEF pose
* raw gripper state

model input:

* normalized state
* instruction

model:

* raw action
* full OFT action chunk
* denormalized action
* processed gripper action

controller:

* actual robot command
* controller feedback

timing:

* image timestamp
* inference start
* inference end
* command sent
* inference latency
* control latency

safety:

* timeout
* hold
* emergency stop

metadata:

* model
* checkpoint
* layout_id
* rollout_id

기존 logging 기능이 있으면 반드시 재사용 또는 확장할 것.

---

# 18. 권장 저장 구조

현재 프로젝트 구조에 맞게 수정해도 되지만 개념적으로는 다음과 같이 구성한다.

sim2real_analysis/

00_config/
01_camera_alignment/
02_paired_scene/
03_state_mapping/
04_features/
05_offline_actions/
06_control_calibration/
07_shadow/
08_prototype/
09_real_rollout/
10_analysis/

기존 프로젝트 내에 더 적절한 구조가 있으면 그것을 따르라.

불필요하게 독립된 새로운 프로젝트를 만들지 말 것.

---

# 19. 지금 바로 해야 할 작업 순서

우선 다음 순서로 작업하라.

STEP 1.
현재 패키지 전체 구조 분석

STEP 2.
OpenVLA Vanilla / OFT의 실제 inference pipeline 추적

STEP 3.
image preprocessing, state preprocessing, action decoding 과정 확인

STEP 4.
Vision Encoder와 projector에서 feature를 추출할 수 있는 위치 확인

STEP 5.
기존 Sim dataset / Real dataset 구조 확인

STEP 6.
기존 Real 데이터에 어떤 정보가 저장되어 있는지 확인

특히:

* RGB
* timestamp
* joint
* EEF pose
* gripper
* planner trajectory
* executed action

존재 여부를 확인

STEP 7.
현재 데이터만으로 가능한 offline Sim-Real analysis 범위를 판단

STEP 8.
필요한 최소 코드 변경 사항 제안

STEP 9.
내 확인 후 코드 구현

단, 명백하게 독립적이고 안전한 분석 코드 추가라면 바로 구현해도 된다.

기존 robot control 동작을 변경하는 코드는 우선 변경 내용을 명확히 설명하고 기존 behavior가 깨지지 않도록 별도 mode 또는 config flag로 추가하라.

---

# 20. 첫 번째 응답에서 원하는 결과

처음부터 대규모 코드를 작성하지 말고 먼저 다음을 보고하라.

## A. Package Overview

현재 프로젝트 구조와 주요 entry point

## B. OpenVLA Vanilla Pipeline

Image
→ preprocessing
→ vision encoder
→ multimodal model
→ action
→ postprocessing

실제 파일/함수 기준

## C. OFT Pipeline

동일하게 실제 파일/함수 기준

특히 K=5 action chunk 구조를 자세히 설명

## D. Existing Dataset

Sim/Real 각각 어떤 데이터가 존재하는지

## E. Missing Information

현재 실험에 필요한데 없는 데이터

## F. Reusable Components

기존 코드에서 그대로 쓸 수 있는 것

## G. Required Modifications

최소 수정 사항

## H. Proposed Experiment Implementation

어떤 파일을 새로 만들거나 수정할 것인지 구체적으로 제시

예:

tools/extract_vla_features.py
tools/analyze_sim_real_distribution.py
tools/run_offline_shadow.py

단, 실제 프로젝트 구조를 본 후 적절한 위치와 이름을 결정할 것.

---

# 최종 원칙

이번 실험의 목적은 Real image를 단순히 Sim image처럼 보이게 만드는 것이 아니다.

최종적으로 다음 관계를 확인하는 것이 목적이다.

Sim/Real Observation Gap
↓
Vision Representation Gap
↓
Action Distribution Gap
↓
Real Control Performance

따라서 analysis code는 이후 camera correction 전/후, preprocessing 변경 전/후, model별 비교를 반복하기 쉽도록 구성하라.

OpenVLA Vanilla와 OFT 모두 동일한 evaluation pipeline을 최대한 공유하도록 구현하고, model-specific 부분만 adapter 형태로 분리하라.

현재 패키지와 기존 기능을 충분히 분석하기 전에는 architecture나 action representation을 추측하여 구현하지 말 것.

# 작업 기록

## 2026-08-26 16:30 KST - Isaac initial frame teacher-forced action test 완료

- 목적: 같은 proprio에서 raw nearest image와 Isaac initial frame이 OFT action을 얼마나 다르게 만드는지 확인.
- 모델:
  - `/home/ubuntu/robot_ws/src/openvla/runs/oftplus_h5_proprio_bounded_oft200_9000--6000_chkpt`
  - variant: `oftplus_h5_proprio`
  - port: `8776`
- 추가 파일:
  - `/home/ubuntu/robot_ws/world_model_baseline/run_oft_isaac_frame_teacher_forced.py`
- 입력:
  - fixed L/C/R, random5, exact training-home validation initial frames
  - 각 frame마다 target XY nearest raw step0 sample
- 비교 조건:
  - raw nearest step0 image + raw step0 proprio
  - Isaac validation initial frame + 같은 raw step0 proprio
  - Isaac validation initial frame + Isaac initial proprio도 추가 저장
- 출력:
  - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_isaac_frame_teacher_forced.json`
- 결과:
  - cases: 9
  - raw image XY cosine to nearest raw GT mean: `0.9956`
  - Isaac image XY cosine to nearest raw GT mean: `0.8754`
  - raw xyz L2 error to GT mean: `0.01108m`
  - Isaac xyz L2 error to GT mean: `0.01342m`
  - Isaac minus raw xyz L2 mean: `0.00715m`
  - Isaac minus raw full-action L2 mean: `0.03357`
- 약한 case:
  - case 3: raw cosine `0.999`, Isaac raw-proprio cosine `0.615`, Isaac-proprio cosine `0.560`
  - case 9: raw cosine `0.992`, Isaac raw-proprio cosine `0.594`, Isaac-proprio cosine `0.504`
  - case 5도 Isaac에서 `0.867`로 하락
- 해석:
  - raw image에서는 같은 모델/같은 raw proprio로 GT XY 방향을 거의 완벽하게 재현.
  - Isaac image로만 바꾸면 일부 case에서 XY 방향성이 크게 약해지거나 틀어진다.
  - Isaac proprio까지 쓰면 약한 case가 더 나빠진다.
  - camera/domain gap은 실제로 model action을 바꾼다.
  - 다만 모든 case가 완전 반대 방향은 아니므로 실패 원인은 camera gap 단독이 아니라 proprio/branch drift와 closed-loop compounding error가 같이 작용한다.
  - 다음 개선 방향은 validation task 계약 불일치 제거, real+sim mixed fine-tune, recovery data 추가.

## 2026-08-26 16:25 KST - Isaac camera vs raw image 비교 완료

- 목적: raw image와 Isaac validation initial frame을 같은 center-crop/224 resize로 비교해 cube 위치, 색상, 크기, 배경 차이 확인.
- 추가 파일:
  - `/home/ubuntu/robot_ws/world_model_baseline/compare_isaac_raw_initial_frames.py`
- 입력 validation:
  - fixed L/C/R, random5, exact training-home `oft200_6000`
- 출력:
  - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_isaac_raw_image_compare/isaac_raw_initial_frame_comparison.json`
  - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_isaac_raw_image_compare/isaac_raw_initial_frame_contact_sheet.jpg`
- 방법:
  - validation target XY와 가장 가까운 raw episode step0을 매칭.
  - 양쪽 이미지를 동일하게 center-crop 후 224x224 resize.
  - raw orange component와 Isaac red component를 connected component 기준으로 추정.
  - contact sheet에 bbox/centroid overlay 저장.
- 수치:
  - compared pairs: 9
  - mean abs RGB diff: `65.70/255`
  - raw orange component pixels mean: `139.9px`
  - Isaac red component pixels mean: `81.4px`
  - Isaac/raw component area ratio mean: `0.60`
  - centroid abs delta mean: `50.1px x, 45.4px y`
  - nearest raw target XY distance mean: `0.0489m`
- 정성 확인:
  - raw는 실제 ZED eye-in-hand + 단일 orange cube + 실제 gripper/테이블 질감.
  - Isaac은 렌더링 + 다중 cube + 강한 그림자 + 다른 gripper/robot appearance.
  - validation target은 JSON상 `red`, instruction은 `Pick up the orange cube.`라 학습 target color/appearance와 다르다.
  - same crop/resize 후에도 cube 위치가 크게 다르고 배경/조명 차이가 큼.
  - segmentation은 Isaac 렌더링 색과 robot part가 섞일 수 있어 contact sheet를 우선 해석.
- 해석:
  - raw/offline에서는 맞는데 Isaac closed-loop에서 첫 XY 방향부터 틀어지는 현상은 camera/domain gap으로 설명 가능성이 높다.
  - 특히 다중 cube와 target color mismatch는 정책 입력 조건 자체가 학습 데이터와 다르다.
  - 다음 우선순위는 6번 teacher-forced Isaac frame test 또는 5번 open-loop model replay.

## 2026-08-26 16:15 KST - rotation action / joint branch 분석 완료

- 목적: offline predicted rotation 3축 vs GT rotation 3축 비교, closed-loop joint4/joint6 branch jump가 rotation action 때문에 생기는지 확인.
- 추가 파일:
  - `/home/ubuntu/robot_ws/world_model_baseline/analyze_oft_rotation_actions.py`
- 입력:
  - offline MAE:
    - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_offline_action_mae_trainval_full_corrected.json`
  - validation:
    - fixed L/C/R, random5, exact training-home `oft200_6000`
- 출력:
  - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_rotation_actions/rotation_action_summary.json`
  - per-run rotation JSON/CSV/PNG:
    - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_rotation_actions/*_rotation.json`
    - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_rotation_actions/*_rotation.csv`
    - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_rotation_actions/*_rotation.png`
- raw rotation distribution:
  - norm mean: `0.1480rad`
  - norm q95/q99/max: `0.4121 / 0.6227 / 2.1354rad`
- offline rotation 복원:
  - all MAE rot x/y/z: `0.01455 / 0.02072 / 0.00882rad`
  - all error norm mean/p95: `0.02913 / 0.16694rad`
  - approach 0~30 error norm mean/p95: `0.04435 / 0.30157rad`
  - all GT/pred corr rot x/y/z: `0.907 / 0.921 / 0.947`
  - approach GT/pred corr rot x/y/z: `0.855 / 0.915 / 0.931`
- closed-loop fixed/random:
  - rotation norm mean/p95: fixed `0.0690 / 0.3487rad`, random `0.0691 / 0.3516rad`
  - joint4 OOD fraction: `0.978`
  - joint6 OOD fraction: `0.129~0.133`
  - joint4 branch jump fraction: `0.933`
  - joint6 branch jump fraction: `0.058~0.059`
  - event: joint6 branch는 step 0부터 이미 `~pi` branch, joint4 OOD는 step 2, joint4 branch jump는 step 6.
- exact training-home:
  - rotation norm mean/p95: `0.0098 / 0.0108rad`
  - joint4 OOD fraction: `0.989`
  - joint6 OOD fraction: `0.433`
  - joint4/joint6 branch jump: step 58/59
- 해석:
  - offline raw에서는 rotation 축 상관이 높으므로 rotation을 전혀 못 배우는 모델은 아니다.
  - 접근 구간의 큰 GT rotation outlier는 모델이 과소예측한다.
  - 일반 home의 joint6 branch 문제는 rotation action 이전부터 시작 pose/proprio branch mismatch로 존재한다.
  - joint4 branch jump는 초반 rotation target과 IK branch 선택 영향을 받을 수 있지만, training-home에서도 작은 rotation action 상태로 나중에 branch jump가 생긴다.
  - 따라서 branch jump를 rotation action 단독 원인으로 단정하지 않는다. IK branch 선택, closed-loop target drift, proprio branch mismatch를 같이 봐야 한다.
  - 다음 우선순위: 4번 Isaac camera vs raw image 차이.

## 2026-08-26 16:05 KST - closed-loop failure timing 분석 완료

- 목적: validation action마다 nearest raw state를 붙이고 step별 proprio OOD, action norm, target distance, gripper, rotation 순서를 확인.
- 추가 파일:
  - `/home/ubuntu/robot_ws/world_model_baseline/analyze_oft_closed_loop_timing.py`
- 입력 validation:
  - fixed L/C/R:
    - `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oftplus_h5_proprio_bounded_oft200_6000_fixed_lcr_closed_loop_after_gtfix_rerun_seed3026.json`
  - random5:
    - `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oftplus_h5_proprio_bounded_oft200_6000_random5_closed_loop_after_gtfix_rerun_seed4026.json`
  - training-home:
    - `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oftplus_h5_proprio_bounded_oft200_6000_training_home_90step_1ep_seed3026.json`
- 출력:
  - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_closed_loop_timing/closed_loop_timing_summary.json`
  - per-run timing JSON/CSV/PNG:
    - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_closed_loop_timing/*_timing.json`
    - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_closed_loop_timing/*_timing.csv`
    - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_closed_loop_timing/*_timing.png`
- 구현:
  - checkpoint `dataset_statistics.json`의 proprio q01/q99로 7D proprio 정규화.
  - raw 9,847 transition 전체에 대해 nearest raw proprio state 계산.
  - validation step별 target XY distance, z offset, XY action norm, XY closing, rotation norm, gripper, proprio OOD, nearest raw distance 기록.
  - episode별 timing plot 저장.
- 확인/정정:
  - `oftplus_h5_proprio_training_home_90step_1ep_seed3026.json`은 server metadata상 이전 `9000--7500` checkpoint였으므로 정식 분석에서 제외.
  - 정확한 `oft200_6000` training-home 파일은 `oftplus_h5_proprio_bounded_oft200_6000_training_home_90step_1ep_seed3026.json`.
- fixed L/C/R 결과:
  - rows: 270, IK success: 1.0
  - target XY start/end mean: `0.3027m -> 0.1796m`
  - mean XY action norm: `0.00240m`
  - mean XY closing: `0.00103m`
  - away fraction: `0.367`
  - XY dead while far fraction: `0.778`
  - rotation norm mean/p95: `0.0690 / 0.3487rad`
  - proprio OOD any: `1.0`
  - nearest raw normalized distance mean: `19.29`
- random5 결과:
  - rows: 450, IK success: 1.0
  - target XY start/end mean: `0.2572m -> 0.1123m`
  - mean XY action norm: `0.00233m`
  - mean XY closing: `0.00129m`
  - away fraction: `0.387`
  - XY dead while far fraction: `0.671`
  - rotation norm mean/p95: `0.0691 / 0.3516rad`
  - proprio OOD any: `1.0`
  - nearest raw normalized distance mean: `19.58`
- exact oft200_6000 training-home 결과:
  - rows: 90, IK success: 1.0
  - target XY start/end: `0.0837m -> 0.1814m`
  - mean XY action norm: `0.00066m`
  - mean XY closing: `-0.00043m`
  - away fraction: `0.956`
  - XY dead while far fraction: `0.856`
  - proprio OOD any: `0.989`
  - nearest raw normalized distance mean: `6.56`
- failure 순서:
  - 일반 home: step 0 proprio OOD/nearest raw OOD와 rotation jump가 먼저 발생.
  - 일반 home: away XY는 episode별 step 5~33, XY dead는 대체로 step 20 이후.
  - 일반 home: gripper close는 step 52~79로 뒤늦게 발생.
  - training-home: step 0 initial proprio는 맞지만 첫 action부터 XY가 target 반대 방향, step 1 proprio OOD, step 3 nearest raw OOD.
- 해석:
  - 실패는 gripper premature close가 1차 원인이 아니다.
  - 일반 home은 proprio/rotation branch mismatch가 먼저 터지고, 이후 XY action collapse와 late gripper가 따라온다.
  - training-home에서도 initial proprio만 맞추면 충분하지 않다. 첫 action 방향성이 이미 나쁘고 closed-loop state가 곧 raw distribution 밖으로 밀린다.
  - 다음 우선순위는 3번 rotation action 문제와 4번 Isaac camera vs raw image 차이 확인이다.

## 2026-08-26 15:22-16:05 KST - oft200_6000 offline action MAE 완료

- 목적: 실행 없이 raw train/val image + proprio를 `oft200_6000` 모델에 넣어 GT action 복원 오차 확인.
- 모델:
  - `/home/ubuntu/robot_ws/src/openvla/runs/oftplus_h5_proprio_bounded_oft200_9000--6000_chkpt`
  - variant: `oftplus_h5_proprio`
  - server port: `8776`
- 추가/수정 파일:
  - `/home/ubuntu/robot_ws/world_model_baseline/run_oft_offline_action_mae.py`
  - `/home/ubuntu/robot_ws/A0509_1-3_WorldModel_실험_파이프라인_결과보고서.md`
- 실행:
  - smoke:
    - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_offline_action_mae_smoke_trainval60.json`
  - full:
    - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_offline_action_mae_trainval_full.json`
  - split corrected:
    - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_offline_action_mae_trainval_full_corrected.json`
- 주의:
  - `steps_with_actions.jsonl`에는 episode 마지막 hold row까지 들어 있어 raw row가 10,067개다.
  - 학습 transition 계약(`observation_t_to_observation_t_plus_1`)과 manifest count에 맞춰 마지막 row는 기본 제외했다.
  - split은 `episode_index < 198`이 아니라 `/home/ubuntu/robot_ws/src/doosan-robot2/raw_dataset_oft_200/metadata/splits.json` 기준으로 보정했다.
- 최종 count:
  - train: 8,875 transitions
  - val: 972 transitions
  - total: 9,847 transitions
- 전체 MAE:
  - x/y/z: `0.000825 / 0.001372 / 0.002297 m`
  - rot x/y/z: `0.01455 / 0.02072 / 0.00882 rad`
  - gripper: `0.03509`
  - xyz L2 MAE: `0.00329 m`
  - rotation L2 MAE: `0.02913 rad`
- split별:
  - train xyz L2 MAE: `0.00330 m`, rotation L2 MAE: `0.02913 rad`, gripper MAE: `0.03520`, XY cosine: `0.9616`
  - val xyz L2 MAE: `0.00317 m`, rotation L2 MAE: `0.02915 rad`, gripper MAE: `0.03407`, XY cosine: `0.9514`
- phase별:
  - approach 0~30: xyz L2 MAE `0.00363 m`, rotation L2 MAE `0.04435 rad`, gripper MAE `0.03088`
  - close 직전 5 step: xyz L2 MAE `0.00202 m`, rotation L2 MAE `0.00766 rad`, gripper MAE `0.09174`
  - close 이후: xyz L2 MAE `0.00310 m`, rotation L2 MAE `0.00819 rad`, gripper MAE `0.02245`
- 해석:
  - raw train/val teacher-forced 조건에서 position action 복원은 mm 단위이고 train/val 차이도 작다.
  - checkpoint가 raw action을 전혀 못 배우거나 GT action scale을 크게 틀리는 상태는 아니다.
  - 접근 구간 rotation 오차와 close 직전 gripper timing 오차는 남아 있어 3번 rotation action 문제, gripper timing 문제로 이어서 봐야 한다.
  - 현재 closed-loop 실패의 큰 축은 raw/offline action MAE보다는 Isaac closed-loop drift, proprio OOD, camera/domain gap 쪽으로 좁혀진다.

## 2026-08-26 13:59 KST - OFT geometry gate 진행 중

- 목적: OFT closed-loop 실패 패턴 중 target 반대 방향 XY action, XY 정렬 전 descent/close 진행을 validation runtime에서 막기.
- 변경 파일:
  - `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/app/a0509_control/vla_validation.py`
  - `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/app/a0509_control_app.py`
  - `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/run_vla_validation.sh`
- 구현:
  - `--vla-enable-geometry-gate` 옵션 추가.
  - target cube pose 기준으로 OFT action 후처리.
  - target 반대 방향 XY action을 target 방향으로 교체.
  - XY 정렬 전 descent 제한.
  - XY 정렬/높이 조건 전 gripper close 차단.
  - validation JSON에 `pre_gate_clipped_action`, `geometry_gate`, `action_was_geometry_gated` 기록.
  - 실행이 중간에 끊겨도 현재 episode의 action tail을 볼 수 있게 `active_episode` snapshot 추가.
- 중간 검증:
  - `python -m py_compile ...` 통과.
  - 12-step smoke:
    - 결과 JSON: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oftplus_h5_proprio_geometry_gate_12step_seed3026.json`
    - 결과: `policy_step_timeout`, 12/12 step geometry gated.
    - raw XY는 target 반대 방향이었고 gate 후 매 step XY distance가 감소.
  - 90-step 1ep 1차:
    - 결과 JSON: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oftplus_h5_proprio_geometry_gate_90step_1ep_seed3026.json`
    - 결과: 실패, `policy_step_timeout`.
    - raw away step 90/90, geometry gated 90/90.
    - 문제: `xy_aligned=0.06m` 아래에서 XY projection이 꺼져 `close_xy=0.05m` 사이 dead zone 발생. Z offset도 0.43m로 높음.
- 추가 조정:
  - XY correction을 `min(xy_aligned, close_xy)`까지 유지.
  - XY가 descent threshold 안에 들어오면 descent를 보강.
  - away XY replacement는 최대 `0.018m/step` 사용.
  - 기본 `geometry_gate_max_descent_m`를 `0.008m`로 상향.
- 현재 실행 중:
  - fast XY/descent gate 90-step 1ep.
  - output: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oftplus_h5_proprio_geometry_gate_fastxy_90step_1ep_seed3026.json`
  - Isaac log: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/isaac_validation_geometry_gate_fastxy_90step_1ep_seed3026.log`
  - OFT server log: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oft_server_geometry_gate_fastxy_90step_1ep_8774.log`
- 2026-08-26 14:01 KST 결과:
  - fast XY/descent gate 90-step 1ep도 실패.
  - 종료 이유: `consecutive_ik_failures`.
  - policy steps: 15.
  - geometry gated: 15/15.
  - IK fail: 5.
  - final target-wrist XY: 0.1439m.
  - final target-wrist Z: 0.6510m.
  - 해석: gate를 크게 걸면 target 방향성은 강제로 만들 수 있지만 planner reachability를 깨서 연속 IK 실패가 난다. 이는 모델 성능 개선이 아니며, 런타임 후처리는 진단/안전장치로만 유지한다.
  - 다음 방향: gate 튜닝보다 OFT 자체가 왜 target 반대 XY action을 내는지 학습 데이터, action 좌표계, normalization/inversion, proprio 입력 정합성을 먼저 잡는다.

## 2026-08-26 14:03 KST - OFT 본체 원인 조사 전환

- 판단: 사용자가 지적한 대로 geometry gate 후처리는 모델 성능 개선이 아니다. gate는 기본 off인 진단/안전장치로만 유지하고, OFT 입력/학습 정합성을 우선 수정한다.
- 확인한 코드:
  - serving: `/home/ubuntu/robot_ws/src/A0509_OFT_selected_training_kit_20260805/openvla-oft/vla-scripts/serve_a0509_oft.py`
  - dataset adapter: `/home/ubuntu/robot_ws/src/A0509_OFT_selected_training_kit_20260805/openvla-oft/prismatic/vla/datasets/a0509_dataset.py`
  - validation proprio: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/app/a0509_control/vla_validation.py`
- 발견:
  - serving은 checkpoint `dataset_statistics.json`으로 action을 unnormalize한 뒤 validation에 그대로 전달한다.
  - validation은 OFT variant가 proprio를 요구하면 현재 Isaac joint state 6개와 gripper closedness 1개를 `state`로 보낸다.
  - checkpoint proprio q01/q99와 validation 첫 step proprio가 크게 다르다.
  - 예: checkpoint q01/q99
    - joint2: -0.653 ~ -0.279
    - joint3: -1.521 ~ -0.898
    - joint5: -1.584 ~ -1.101
    - joint6: -0.297 ~ 0.513
  - validation 첫 step proprio 예:
    - `[-3.188, -0.021, -0.716, 0.022, -1.981, -3.124, 0.00006]`
  - 즉 joint2, joint3, joint5, joint6 등이 학습 범위 밖이고 normalization 후 clip된다.
- 현재 가설:
  - closed-loop에서 OFT가 target 반대 XY action으로 고정되는 핵심 원인은 action head 자체보다 runtime proprio 좌표계/offset/order mismatch일 가능성이 높다.
- 다음 작업:
  - raw dataset `joint_position_rad` 생성 기준과 Isaac runtime `get_joint_positions()` 기준을 비교.
  - 특히 joint6이 학습에서는 -0.3~0.5인데 runtime에서는 -3.1 근처인 offset/wrap 문제를 먼저 확인.
  - validation에서 학습과 같은 proprio representation을 보내도록 수정하거나, dataset을 runtime representation으로 재생성/재학습한다.
- 2026-08-26 14:06 KST 조치:
  - OFT validation 전용 home pose 추가.
  - 파일: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/app/config/a0509_vla_oft_training_home_pose.json`
  - 값은 raw dataset train first-step 90개 중 평균 시작 joint pose에 가장 가까운 실제 episode pose 사용.
  - source episode: `episode_000057`, step 0.
  - joints: `[-3.09236, -0.51718, -1.19633, 0.02577, -1.40699, 0.10139]`
  - 목적: 모델 입력 proprio가 학습 분포 안에 들어오도록 validation 초기 pose를 맞춘다.
- 2026-08-26 14:07 KST 검증 결과:
  - 실행: gate off, training-home pose, fixed LCR 1ep, 90 policy steps.
  - 결과 JSON: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oftplus_h5_proprio_training_home_90step_1ep_seed3026.json`
  - Isaac log: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/isaac_validation_training_home_90step_1ep_seed3026.log`
  - 결과: 실패, `policy_step_timeout`.
  - IK success: 90/90.
  - geometry gate: off, 0/90.
  - first proprio: `[-3.0936, -0.5211, -1.1965, 0.0258, -1.4070, 0.1014, 0.0001]`
  - final target-wrist XY: 0.2570m.
  - final target-wrist Z: 0.2495m.
  - target lift: 0.0m.
  - mean XY cosine to target: 0.239.
  - target 반대 XY steps: 30/90.
  - 해석:
    - training-home pose로 바꾸면 proprio OOD와 IK failure는 사라진다.
    - 하지만 모델 자체 접근 성능은 아직 부족하다. XY가 0.280m -> 0.257m로 거의 줄지 않았고, Z도 0.286m -> 0.249m에 그쳤다.
    - 따라서 다음 문제는 runtime 후처리가 아니라 OFT 학습/데이터 품질이다.
- 다음 작업:
  - validation home pose 변경은 유지하고, gate는 계속 off로 평가한다.
  - raw dataset에서 fixed LCR과 유사한 target 상대 위치별 첫/중간 action 분포를 분석한다.
  - OFT가 target 위치를 이미지에서 못 쓰는지 확인하기 위해 same proprio + different cube layout에서 predicted action sensitivity를 측정한다.
  - 필요하면 training set을 더 크게 만들거나, target-relative proprio/pose feature를 넣은 OFT variant로 재학습한다.
- 2026-08-26 14:08 KST 스크립트 정리:
  - `run_vla_validation.sh`에 `A0509_VLA_HOME_CONFIG` 추가.
  - 기본값: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/app/config/a0509_vla_oft_training_home_pose.json`
  - 이유: 일반 app home pose는 학습 proprio 분포 밖이므로 VLA/OFT validation에는 부적합하다.

## 2026-08-26 14:10 KST - raw dataset vs validation action geometry 비교

- 추가 파일:
  - `/home/ubuntu/robot_ws/world_model_baseline/analyze_oft_dataset_vs_validation_geometry.py`
- 출력:
  - `/home/ubuntu/robot_ws/world_model_baseline/oft_dataset_vs_training_home_validation_geometry.json`
- 검증:
  - `python -m py_compile robot_ws/world_model_baseline/analyze_oft_dataset_vs_validation_geometry.py` 통과.
- 비교 대상:
  - raw: `/home/ubuntu/robot_ws/src/doosan-robot2/raw_dataset_oft_200`, 10,067 rows.
  - validation: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oftplus_h5_proprio_training_home_90step_1ep_seed3026.json`, 90 rows.
- 핵심 결과:
  - raw 전체:
    - mean XY cosine to target: 1.0
    - away fraction: 0.0
    - mean XY action norm: 0.00669m
    - mean XY closing: 0.00669m/step
  - raw alignment phase:
    - rows: 3,871
    - mean XY distance: 0.159m
    - mean XY action norm: 0.01693m
    - mean XY closing: 0.01693m/step
    - away fraction: 0.0
  - raw `240mm_plus` distance bin:
    - rows: 1,079
    - mean XY distance: 0.311m
    - mean XY action norm: 0.01818m
    - mean XY closing: 0.01818m/step
    - away fraction: 0.0
  - validation:
    - rows: 90
    - mean XY distance: 0.260m
    - mean XY action norm: 0.00232m
    - mean XY closing: 0.00060m/step
    - mean XY cosine to target: 0.239
    - away fraction: 0.333
- 해석:
  - raw dataset action 자체는 target-relative XY 방향성이 정상이다. action 좌표계 부호가 dataset에서 뒤집혀 있다는 증거는 없다.
  - validation 초기 거리와 가장 유사한 raw `240mm_plus` 구간은 18mm/step 수준으로 target에 접근하지만, OFT closed-loop 출력은 0.6mm/step에 그친다.
  - training-home pose로 proprio OOD를 줄인 뒤에도 action magnitude와 target 방향성이 크게 죽는다.
  - 다음 우선순위는 이미지/언어/FiLM 또는 checkpoint 학습 상태가 target 위치를 실제로 쓰는지 확인하는 sensitivity test다.

## 2026-08-26 14:14 KST - OFT image sensitivity test (모델 mismatch로 해석 제외)

- 추가 파일:
  - `/home/ubuntu/robot_ws/world_model_baseline/run_oft_image_sensitivity.py`
- 출력:
  - `/home/ubuntu/robot_ws/world_model_baseline/oft_image_sensitivity_fixed_state_step0.json`
- 검증:
  - `python -m py_compile robot_ws/world_model_baseline/run_oft_image_sensitivity.py` 통과.
- 실행:
  - OFT 서버를 port 8776에서 `oftplus_h5_proprio` / `oftplus_h5_proprio_bounded_9000--7500_chkpt`로 기동.
  - 주의: 이 checkpoint는 앞선 `oft200_6000` validation 기준 모델이 아니다.
  - 기준 validation 모델은 `/home/ubuntu/robot_ws/src/openvla/runs/oftplus_h5_proprio_bounded_oft200_9000--6000_chkpt`.
  - GPU 접근이 필요해서 sandbox 밖에서 실행.
  - raw dataset step 0 이미지 9개를 사용.
  - proprio는 모두 동일하게 고정:
    - `[-3.09236, -0.51718, -1.19633, 0.02577, -1.40699, 0.10139, 0.0]`
  - instruction은 모두 `Pick up the orange cube.`
  - 테스트 후 `/shutdown`으로 서버 정상 종료.
- 결과:
  - cases: 9
  - mean predicted action xyz: `[-0.001896, 0.000132, -0.002196]`
  - std predicted action xyz:
    - x: `0.0000074m`
    - y: `0.0000074m`
    - z: `0.0000150m`
  - mean predicted XY action norm: `0.00190m`
  - std predicted XY action norm: `0.0000077m`
  - max pairwise full-action L2: `0.000916`
  - 예시:
    - `episode_000001` grid `(0,0)` dataset xyz `[0.0033, -0.0088, -0.0115]`, predicted xyz `[-0.0019, 0.0001, -0.0022]`
    - `episode_000051` grid `(0,5)` dataset xyz `[0.0032, 0.0092, -0.0111]`, predicted xyz `[-0.0019, 0.0001, -0.0022]`
    - y 방향이 반대인 데이터 이미지에서도 prediction은 거의 동일.
- 해석:
  - 모델이 달라서 위 sensitivity 수치는 `oft200_6000` closed-loop 실패 원인 근거로 사용하지 않는다.
  - 스크립트 자체는 재사용 가능하지만, 반드시 `/home/ubuntu/robot_ws/src/openvla/runs/oftplus_h5_proprio_bounded_oft200_9000--6000_chkpt`로 다시 실행해야 한다.

## 2026-08-26 14:19 KST - 정확한 oft200_6000 모델로 image sensitivity 재실행

- 사용자 확인 기준:
  - checkpoint: `/home/ubuntu/robot_ws/src/openvla/runs/oftplus_h5_proprio_bounded_oft200_9000--6000_chkpt`
  - Isaac Sim: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806`
  - 분석/스크립트: `/home/ubuntu/robot_ws/world_model_baseline`
  - 데이터:
    - `/home/ubuntu/robot_ws/tensorflow_datasets_oft_200_a0509`
    - `/home/ubuntu/robot_ws/src/doosan-robot2/raw_dataset_oft_200`
- 실행:
  - serving code는 `/home/ubuntu/robot_ws/src/A0509_OFT_selected_training_kit_20260805/openvla-oft`의 `prepare_and_serve_a0509_variant.sh` 사용.
  - checkpoint만 `/home/ubuntu/robot_ws/src/openvla/runs/oftplus_h5_proprio_bounded_oft200_9000--6000_chkpt`로 지정.
  - port 8776, GPU 0, variant `oftplus_h5_proprio`.
  - 서버 ready checkpoint도 위 경로로 확인.
  - 실행 후 `/shutdown`으로 정상 종료.
- 출력:
  - fixed proprio:
    - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_image_sensitivity_fixed_state_step0.json`
  - dataset proprio:
    - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_image_sensitivity_dataset_state_step0.json`
- fixed proprio 결과:
  - cases: 9
  - mean predicted action xyz: `[0.00933, -0.00096, -0.02077]`
  - std predicted action xyz: `[0.00197, 0.00961, 0.00081]`
  - mean XY action norm: `0.01342m`
  - max pairwise full-action L2: `0.11035`
  - dataset GT XY vs predicted XY mean cosine: `0.980`
- dataset proprio 결과:
  - cases: 9
  - mean predicted action xyz: `[0.00732, -0.00143, -0.02179]`
  - std predicted action xyz: `[0.00193, 0.01042, 0.00126]`
  - mean XY action norm: `0.01244m`
  - max pairwise full-action L2: `0.07201`
  - dataset GT XY vs predicted XY mean cosine: `0.996`
- 해석 수정:
  - 정확한 `oft200_6000` 모델은 raw dataset step 0 이미지/상태에 대해 이미지 위치 변화에 분명히 반응한다.
  - 따라서 “모델이 이미지를 거의 안 본다”는 이전 결론은 잘못된 모델에서 나온 것이므로 폐기한다.
  - raw dataset 초기 sample에서는 OFT가 target-relative XY 방향을 잘 재현한다.
  - closed-loop 실패 원인은 Isaac validation 입력 이미지/카메라/색/center crop/초기 pose/좌표계 또는 closed-loop state가 raw training 분포와 어긋나는 쪽으로 다시 좁혀야 한다.
- 다음 작업:
  - Isaac validation initial frame을 정확한 `oft200_6000` 서버에 넣고, raw dataset image sensitivity와 같은 방식으로 action을 비교한다.
  - validation initial frame의 orange cube crop/색/해상도/center-crop 후 위치가 raw dataset 이미지와 같은 분포인지 확인한다.
  - training-home pose가 `oft200_6000` checkpoint의 proprio q01/q99와 맞는지 다시 계산한다.

## 2026-08-26 14:20 KST - 정확한 oft200_6000 validation geometry 재분석

- 출력:
  - fixed LCR:
    - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_dataset_vs_fixed_lcr_after_gtfix_rerun_geometry.json`
  - random5:
    - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_dataset_vs_random5_after_gtfix_rerun_geometry.json`
- fixed LCR 전체:
  - validation rows: 270
  - mean XY distance: `0.205m`
  - mean XY action norm: `0.00240m`
  - mean XY closing: `0.00103m/step`
  - mean XY cosine: `0.257`
  - away fraction: `0.367`
  - close fraction: `0.200`
- fixed LCR first 20:
  - mean XY action norm: `0.00914m`
  - mean XY closing: `0.00833m/step`
  - mean XY cosine: `0.910`
  - away fraction: `0.0`
- random5 전체:
  - validation rows: 450
  - mean XY distance: `0.131m`
  - mean XY action norm: `0.00233m`
  - mean XY closing: `0.00129m/step`
  - mean XY cosine: `0.142`
  - away fraction: `0.387`
  - close fraction: `0.318`
- random5 first 20:
  - mean XY action norm: `0.00919m`
  - mean XY closing: `0.00834m/step`
  - mean XY cosine: `0.911`
  - away fraction: `0.0`
- 에피소드 구간별 패턴:
  - 초반 20 step은 대체로 target 방향으로 8-9mm/step 접근.
  - 중후반에는 XY action norm이 `0.0003-0.0005m` 수준으로 줄고, 일부 episode에서 away fraction이 1.0까지 증가.
  - gripper close는 후반에 0.5-1.0 fraction까지 올라가지만 lift/success로 이어지지 않음.
- 해석 수정:
  - 정확한 `oft200_6000` 모델은 raw step 0에서도, validation 초반에서도 target 방향성을 낸다.
  - 핵심 실패는 “처음부터 이미지를 못 봄/항상 반대 방향”이 아니라 `closed-loop 중반 이후 action magnitude collapse + close 상태 정체`.
  - 다음은 validation trajectory에서 어느 시점부터 proprio/image가 train 분포 밖으로 나가는지 확인해야 한다.

## 2026-08-26 14:33 KST - action collapse 원인 확인

- 추가 파일:
  - `/home/ubuntu/robot_ws/world_model_baseline/analyze_oft_closed_loop_distribution.py`
- 출력:
  - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_fixed_lcr_closed_loop_distribution.json`
  - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_random5_closed_loop_distribution.json`
  - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_training_home_closed_loop_distribution.json`
  - `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_dataset_vs_training_home_geometry.json`
- 검증:
  - `python -m py_compile /home/ubuntu/robot_ws/world_model_baseline/analyze_oft_closed_loop_distribution.py` 통과.
- checkpoint proprio q01/q99:
  - q01: `[-3.793, -0.919, -1.854, -0.075, -1.981, -0.597, 0.0]`
  - q99: `[-2.483, 0.052, -0.566, 0.086, -1.000, 0.713, 1.0]`
- 기존 fixed/random validation 분석:
  - raw phase별 OOD any는 7-17% 수준.
  - 기존 validation은 OOD any가 100%.
  - 중후반 validation은 joint3와 joint4가 100% q01/q99 밖.
  - 특히 joint4는 raw mean이 `~0.004-0.009rad`인데 validation 중후반은 `3.4-3.7rad` 수준.
  - 이는 joint4 wrap/표현 mismatch 또는 validation trajectory가 학습 관절분포에서 크게 벗어났다는 뜻.
- 정확한 `oft200_6000` + `a0509_vla_oft_training_home_pose.json` 재실행:
  - output: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oftplus_h5_proprio_bounded_oft200_6000_training_home_90step_1ep_seed3026.json`
  - Isaac log: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/isaac_validation_oft200_6000_training_home_90step_1ep_seed3026.log`
  - server log: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oft_server_oft200_6000_training_home_90step_1ep_8777.log`
  - 결과: 실패, `policy_step_timeout`, 90 steps.
  - final target-wrist XY: `0.181m`
  - final target-wrist Z: `0.120m`
  - target lift: `~0.0m`
- training-home 재실행의 핵심 수치:
  - validation 전체 mean XY action norm: `0.00066m`
  - validation mean XY cosine: `-0.574`
  - away fraction: `0.956`
  - close fraction: `0.0`
  - first 20 mean XY action norm: `0.00065m`
  - first 20 away fraction: `0.95`
  - proprio OOD any: `0.989`
  - OOD dims:
    - joint4: `0.989`
    - joint5: `0.822`
    - joint6: `0.433`
- 중요한 발견:
  - `a0509_vla_oft_training_home_pose.json`의 joint pose는 raw dataset step 0 pose가 아니다.
  - current config:
    - `[-3.09236, -0.51718, -1.19633, 0.02577, -1.40699, 0.10139]`
  - raw dataset 실제 first step 예:
    - `episode_000001`: joints `[-3.1858, -0.0185, -0.7153, 0.0219, -1.9810, 0.0001]`, EEF `[0.3049, -0.0117, 0.7239]`
    - `episode_000057`: joints `[-3.1865, -0.0185, -0.7152, 0.0219, -1.9811, -0.0001]`, EEF `[0.3049, -0.0119, 0.7239]`
  - 현재 training-home 실행의 initial EEF:
    - `[0.568, 0.030, 1.147]`
  - 즉 validation start가 raw collection start와 다르다.
- 더 중요한 action coverage 문제:
  - raw dataset 전체 10,067 rows에서 `delta_x < 0` row가 0개.
  - action statistics도 `delta_x` min/q01이 `0.0`.
  - 따라서 OFT는 base-frame `-X` 이동을 학습/복원할 수 없다.
  - training-home 재실행에서는 target cube가 wrist보다 작은 X에 있음:
    - step 0 wrist XY: `[0.568, 0.030]`
    - target XY: `[0.485, 0.040]`
    - target vector: `[-0.083, +0.010]`
  - 필요한 action은 `-X`, 하지만 모델 출력은 `[+0.0065, -0.0052]`로 반대 방향.
  - 후반에는 raw action이 `x=-0.0001` 정도로 나오더라도 q01/min 때문에 validation clipping에서 `x=0.0`으로 잘림.
- 결론:
  - action이 작은 것은 결과 증상이다.
  - 기존 fixed/random validation 실패는 `-X` coverage만으로 설명되지 않는다.
    - fixed/random의 모든 episode는 초기 `target_x > wrist_x`라서 `+X` 접근이 필요한 케이스였지만 실패했다.
    - 예: fixed LCR ep1 `target_x=0.540`, `wrist0_x=0.310`, `need_dx=+0.230`.
    - random5 ep1 `target_x=0.486`, `wrist0_x=0.310`, `need_dx=+0.176`.
  - 기존 fixed/random 실패의 핵심 원인은 closed-loop 중반 이후 validation proprio가 학습 분포 밖으로 벗어나며 action magnitude가 collapse되는 것.
    - 특히 joint4/joint3가 중후반에 q01/q99 밖으로 100% 벗어남.
    - default-home first proprio도 joint6이 raw dataset의 `~0rad`와 달리 `-3.124rad`라 학습 표현과 다름.
  - `-X` coverage 문제는 training-home 재실행에서 추가로 드러난 별도 문제다.
    - training-home 실행은 `target_x=0.485`, `wrist0_x=0.568`, `need_dx=-0.083`이라 dataset에 없는 `-X` 접근이 필요했다.
    - raw dataset 전체에서 `delta_x < 0` row가 0개이므로 이 layout/start에서는 모델이 구조적으로 불리하다.
- 다음 조치:
  - 우선 기존 fixed/random 실패를 잡기 위해 validation proprio를 raw dataset 표현과 맞춘다.
    - joint6 `-3.124rad` vs raw `~0rad` wrap/offset 문제 확인.
    - IK가 joint4를 `~3rad` branch로 보내지 않도록 seed/branch/pose constraint를 raw trajectory와 맞춘다.
  - 그 다음 `a0509_vla_oft_training_home_pose.json`를 raw first-step joint pose로 교체하거나, validation layout을 raw dataset과 같이 wrist보다 target X가 항상 큰 영역으로 제한해 sanity check.
  - 근본적으로는 dataset을 양방향 X/Y 접근이 포함되도록 다시 수집/생성하거나, action을 end-effector/camera-relative frame 또는 target-relative feature 기반으로 재학습해야 함.

## 2026-08-26 15:00 KST - validation proprio/IK branch 1차 정합화

- 사용자 지정 기준:
  - checkpoint: `/home/ubuntu/robot_ws/src/openvla/runs/oftplus_h5_proprio_bounded_oft200_9000--6000_chkpt`
  - Isaac Sim: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806`
  - 데이터:
    - `/home/ubuntu/robot_ws/tensorflow_datasets_oft_200_a0509`
    - `/home/ubuntu/robot_ws/src/doosan-robot2/raw_dataset_oft_200`
- 변경 파일:
  - `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/app/a0509_control/lula_planner.py`
  - `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/app/a0509_control/vla_validation.py`
  - `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/app/a0509_control_app.py`
  - `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/run_vla_validation.sh`
  - `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/app/config/a0509_vla_oft_training_home_pose.json`
- 조치:
  - validation IK를 현재 관절값 `warm_start` 기반 Lula IK로 변경해서 closed-loop 중 같은 관절 branch를 유지하도록 함.
  - OFT proprio 입력에 checkpoint `proprio` q01/q99 중심 기준 2π branch wrapping 추가.
  - JSON action record에 `raw_proprio_state`를 추가해 Isaac raw joint와 모델 입력 joint를 분리 기록.
  - `--vla-proprio-joint-offsets-rad J1 ... J6` 옵션 추가. 필요하면 joint6 `-π -> 0` 케이스에 `0 0 0 0 0 3.141592653589793`를 지정해 검증 가능.
  - `run_vla_validation.sh` 기본 checkpoint를 정확한 `oft200_6000` 경로로 변경.
  - VLA home pose를 raw dataset first-step 평균 branch로 교체:
    - `[-3.18604, -0.01845, -0.71542, 0.02187, -1.98101, 0.00003]`
- 검증:
  - `python -m py_compile` 통과:
    - `vla_validation.py`
    - `a0509_control_app.py`
    - `lula_planner.py`
  - `bash -n run_vla_validation.sh` 통과.
- 다음 검증:
  - gate off, fixed LCR 1ep/90step 재실행.
  - 결과에서 `raw_proprio_state`와 `proprio_state`를 비교해 joint4가 더 이상 3.x branch로 튀지 않는지 확인.
- 1차 재실행 결과:
  - output: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oft200_6000_proprio_branchfix_90step_1ep_seed3026.json`
  - 결과: 실패, `policy_step_timeout`, 90/90 IK success.
  - raw first proprio: `[-3.1879, -0.0210, -0.7156, 0.0218, -1.9810, 0.0000, 0.0]`
  - raw last proprio: `[-3.6531, -0.1467, -1.7468, 3.1336, -0.4844, 4.1472, 0.9647]`
  - 해석: 현재 관절값 warm-start만으로는 충분하지 않음. 모델 rotation action이 wrist orientation을 계속 돌리며 joint4/joint6 branch가 크게 튐.
- 추가 조치:
  - `--vla-lock-ik-orientation` 옵션 추가.
  - `run_vla_validation.sh` 기본값은 다시 `A0509_VLA_LOCK_IK_ORIENTATION=0`으로 유지.
  - 목적: OFT closed-loop validation에서 위치 delta만 우선 평가하고 wrist orientation 누적으로 인한 branch jump를 차단.
  - 주의: 이는 모델 출력 후처리에 해당하므로 정식 성능 평가용이 아니라 원인 분리 diagnostic 전용이다.
- orientation lock 재실행 결과:
  - output: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oft200_6000_proprio_branchfix_lockrot_90step_1ep_seed3026.json`
  - 결과: 실패, `policy_step_timeout`, 90/90 IK success.
  - final target-wrist XY: `0.1487m`
  - final target-wrist Z: `0.1877m`
  - target lift: `0.0m`
  - raw first proprio: `[-3.1879, -0.0210, -0.7156, 0.0218, -1.9810, 0.0000, 0.0]`
  - raw last proprio: `[-3.3777, -0.3256, -1.8598, 0.2757, -0.3324, -0.3807, 0.9855]`
  - proprio OOD any: `0.878`
  - OOD dims: joint3 `0.544`, joint4 `0.878`, joint5 `0.789`
  - 해석:
    - joint4 `3.x` branch와 joint6 `4.x/-2.x` wrap 문제는 크게 줄었지만, trajectory가 여전히 checkpoint q01/q99 밖으로 나간다.
    - 특히 joint4 q99 `0.086rad` 대비 last `0.276rad`, joint5 q99 `-1.000rad` 대비 last `-0.332rad`.
    - 따라서 joint branch mismatch만으로는 해결이 끝나지 않았고, validation start/layout/height 또는 action Z/close timing이 raw trajectory 분포와 아직 다르다.
    - 단, orientation lock 결과는 모델 성능 개선으로 해석하지 않는다. 후처리 없이 gate off/lock off 결과를 기준으로 판단한다.

## 2026-08-26 15:02 KST - 후처리 없이 state/IK 정합성만 적용한 확인

- 실행 조건:
  - checkpoint: `/home/ubuntu/robot_ws/src/openvla/runs/oftplus_h5_proprio_bounded_oft200_9000--6000_chkpt`
  - output: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oft200_6000_state_ikmatch_only_90step_1ep_seed3026.json`
  - geometry gate: off
  - orientation lock: off
  - proprio offset: `[0, 0, 0, 0, 0, 0]`
  - home pose: raw dataset first-step 평균 branch
  - 적용한 정합성:
    - OFT 입력 proprio를 checkpoint `proprio` q01/q99 기준 2π branch로 wrapping.
    - IK는 현재 관절값 warm-start 사용.
- 결과:
  - 실패, `policy_step_timeout`.
  - policy steps: 90.
  - IK success: 90/90.
  - final target-wrist XY: `0.3130m`.
  - final target-wrist Z: `0.3927m`.
  - target lift: `0.0m`.
  - first raw proprio:
    - `[-3.1879, -0.0210, -0.7156, 0.0218, -1.9810, 0.0000, 0.0]`
  - last raw proprio:
    - `[-3.6608, -0.2145, -1.7916, 3.1241, -0.6340, 4.3315, 0.9802]`
  - last model proprio after wrapping:
    - `[-3.6608, -0.2145, -1.7916, 3.1241, -0.6340, -1.9517, 0.9802]`
  - proprio OOD any: `0.989`.
  - OOD dims:
    - joint1 `0.056`
    - joint3 `0.789`
    - joint4 `0.989`
    - joint5 `0.289`
    - joint6 `0.967`
  - mean XY action norm: `0.00291m`.
  - close mean: `0.589`.
- 해석:
  - validation 시작 proprio는 raw first-step branch와 맞음.
  - 하지만 모델이 낸 action을 그대로 실행하면 closed-loop 중 joint4/joint6 branch가 다시 raw 학습 분포 밖으로 감.
  - action/output 후처리 없이 보면 실패가 유지됨.
  - 따라서 현재 `oft200_6000`의 closed-loop 실패는 단순 validation 입력 표현 mismatch만으로 해결되지 않음. 모델/데이터/closed-loop 분포 문제가 남아 있다.
  - 이 상태에서 geometry gate 또는 orientation lock으로 성공처럼 만드는 것은 평가 왜곡이므로 사용하지 않는다.

## 2026-08-26 15:05 KST - 결과보고서 최종판 수정

- 수정 파일:
  - `/home/ubuntu/robot_ws/A0509_1-3_WorldModel_실험_파이프라인_결과보고서.md`
- 반영한 최종 기준:
  - 정확한 모델은 `/home/ubuntu/robot_ws/src/openvla/runs/oftplus_h5_proprio_bounded_oft200_9000--6000_chkpt`.
  - Isaac Sim은 `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806`.
  - 기준 데이터는 `/home/ubuntu/robot_ws/tensorflow_datasets_oft_200_a0509`, `/home/ubuntu/robot_ws/src/doosan-robot2/raw_dataset_oft_200`.
  - 시작 pose 고정 + cube 위치 변경으로 수집한 실험이므로 시작 pose 다양성 부족을 주원인으로 쓰지 않음.
  - raw/offline image sensitivity에서는 `oft200_6000` 모델이 cube 위치에 반응함.
  - 최종 평가는 `geometry gate off`, `orientation lock off`, action/output 후처리 없음.
  - 허용한 보정은 raw first-step home pose, proprio joint 표현/wrap 정합화, IK warm-start처럼 학습/runtime 입력 계약을 맞추는 범위로 제한.
  - 이 조건에서도 `oft200_6000_state_ikmatch_only_90step_1ep_seed3026.json` 결과가 `policy_step_timeout`으로 실패.
- 최종 결론:
  - GT replay와 Isaac 파이프라인은 큰 문제에서 제외.
  - 현재 `oft200_6000` checkpoint는 raw first-step에서는 반응하지만 closed-loop에서는 자기 action 누적으로 proprio가 학습 분포 밖으로 밀리고 grasp/lift에 실패.
  - 후처리로 성공처럼 만드는 것은 평가 왜곡이므로 사용하지 않음.
  - 현재 병목은 validation gain/timeout이 아니라 모델/데이터의 closed-loop 일반화 성능.

## 2026-08-26 16:50 KST - 3-cube orange 임시 소규모 검증 시도

- 사용자 지시:
  - 이후 사용자가 정정: 이 조건은 잘못 이해한 것.
  - 실제 의도는 기존 3개/red가 아니라 학습 조건처럼 이번 실험만 1개/orange로 맞추는 것.
- 실행 조건:
  - checkpoint: `/home/ubuntu/robot_ws/src/openvla/runs/oftplus_h5_proprio_bounded_oft200_9000--6000_chkpt`
  - Isaac Sim: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806`
  - layout manifest: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/app/config/a0509_vla_fixed_lcr_seed3026.json`
  - output: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oft200_6000_three_orange_fixed_lcr_3ep_seed3026.json`
  - `A0509_VLA_SINGLE_TARGET_CUBE=0`
  - instruction: `Pick up the orange cube.`
  - `--red-cube-label orange`
  - `--red-cube-display-color 1.0 0.42 0.0`
- 결과:
  - OFT server ready, Isaac app started, camera ready, validation started.
  - 그러나 OFT `/health`의 `request_count`가 계속 `0`으로 유지되어 policy rollout에 진입하지 못함.
  - 결과 JSON은 `status=running`, `phase=opening_for_reset`, `completed=0` 상태까지만 기록됨.
  - 성능 결과로 사용하지 않음.
- 정리:
  - 실행 중단 및 OFT `/shutdown` 호출.
  - 남은 `run_vla_validation`, OFT server, Isaac python 프로세스 없음 확인.
  - orange color/label은 Isaac session layer와 실행 인자에만 적용했으므로 종료와 함께 원 USD에는 저장되지 않음.

## 2026-08-26 17:03 KST - 1-cube orange 임시 소규모 검증 재시도

- 정정된 실험 의도:
  - 학습 조건과 맞추기 위해 이번 실험만 target cube 1개만 보이게 함.
  - target red cube prim은 orange label/display color로 실행.
  - non-target cube는 `--vla-single-target-cube` 경로로 시야/워크스페이스 밖으로 이동.
  - 실행 후 임시 코드 보조 패치와 실행 상태 원복.
- 실행 조건:
  - checkpoint: `/home/ubuntu/robot_ws/src/openvla/runs/oftplus_h5_proprio_bounded_oft200_9000--6000_chkpt`
  - Isaac Sim: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806`
  - layout manifest: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/app/config/a0509_vla_fixed_lcr_seed3026.json`
  - output: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oft200_6000_single_orange_fixed_lcr_3ep_seed3026_activeik_rerun2.json`
  - `A0509_VLA_SINGLE_TARGET_CUBE=1`
  - instruction: `Pick up the orange cube.`
  - `--red-cube-label orange`
  - `--red-cube-display-color 1.0 0.42 0.0`
- 임시 보조 패치:
  - single-cube 모드에서 non-target cube를 밖으로 보낼 때 전체 cube IK 검증 때문에 layout이 reject되는 문제를 피하려고,
    `validate_auto_targets(active_colors)` 형태로 target cube만 검증하도록 임시 적용.
  - 실험 종료 후 원래 전체 검증 호출로 복원.
- 결과:
  - OFT server ready.
  - Isaac app started, camera ready, validation started.
  - 하지만 4분 이상 대기해도 OFT `/health`의 `request_count`가 계속 `0`.
  - 결과 JSON은 `status=running`, `phase=opening_for_reset`, `completed=0` 상태까지만 기록.
  - policy rollout에 진입하지 못했으므로 성능 결과로 사용하지 않음.
- 정리:
  - OFT `/shutdown` 호출 후 실험 프로세스 종료.
  - 남은 `run_vla_validation`, OFT server, Isaac python 프로세스 없음 확인.
  - 임시 active-target IK 패치는 원복했고 `py_compile` 통과.
  - orange color/label은 session layer와 실행 인자에만 적용되어 원 USD에는 저장되지 않음.

## 2026-08-26 17:10 KST - 1-cube orange 미진입 원인 확인

- 목적:
  - 이전 1-cube/orange 실행이 policy rollout까지 못 간 원인 확인.
- 진단 방법:
  - VLA validation phase transition 로그를 임시 추가.
  - single-cube 모드 layout 검증용 target-only IK 임시 패치 적용.
  - 1 episode, `--steps 3000` 제한으로 실행.
- output:
  - `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oft200_6000_single_orange_phase_debug_1ep_seed3026.json`
  - `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/isaac_validation_oft200_6000_single_orange_phase_debug_1ep_seed3026.log`
- 확인된 phase:
  - `opening_for_reset -> request_home`
  - `request_home -> waiting_for_home`
  - 이후 3000 steps 동안:
    - `phase=waiting_for_home`
    - `runtime_mode=home`
    - `runtime_phase=move_home`
  - policy request는 0회.
- 결론:
  - 1-cube/orange validation은 `opening_for_reset`이 아니라 home reset/home motion 단계에서 멈춤.
  - layout 배치, camera frame, OFT inference까지 도달하지 못했으므로 policy 성능 확인 불가.
  - 다음 조치는 VLA validation의 home reset 경로를 점검해야 함:
    - `start_home_motion()`이 raw first-step home pose로 수렴하지 못하는지
    - home trajectory 완료 조건이 headless/uncapped sim에서 충족되지 않는지
    - 강제 home reset fallback을 diagnostic으로 허용할지 검토.
- 정리:
  - 진단 로그 패치와 target-only IK 임시 패치 모두 원복.
  - `py_compile` 통과.
  - 남은 Isaac/OFT 프로세스 없음.

## 2026-08-26 17:27 KST - 1-cube orange validation setup 경로 수정 및 재검증

- 사용자 해석 정정:
  - 색 자체가 핵심 병목은 아니었음.
  - 학습 조건과 맞추는 핵심은 이번 validation에서 target cube 1개만 보이게 하는 것.
- 확인한 추가 원인:
  - VLA validation의 home reset은 일반 `start_home_motion()`을 호출해 `motion_speed_scale=0.428174...`로 home RRT trajectory를 실행했고, 이전 3000-step 제한에서는 `runtime_phase=move_home`에 계속 머물렀음.
  - single-target layout 검증 코드가 `validate_auto_targets()` 결과를 color key처럼 필터링하고 있었음.
    - 실제 key는 `pregrasp`, `grasp`, `lift`.
    - 따라서 single-target 모드에서 IK가 성공해도 `red` key 조회가 `False`가 되어 layout rejection이 발생할 수 있었음.
- 수정 파일:
  - `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/app/a0509_control/isaac_runtime.py`
  - `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/app/a0509_control/vla_validation.py`
- 수정 내용:
  - `start_home_motion(..., setup_motion_speed_scale=None)` optional 인자 추가.
  - VLA validation home reset에서만 `setup_motion_speed_scale=4.0`으로 setup trajectory를 계획.
  - VLA validation `waiting_for_home`에 20초/1200-step timeout fallback 추가.
  - single-target layout 검증에서 잘못된 color-key IK 필터 제거.
  - policy rollout 자체의 action/gain, geometry gate, orientation lock은 변경하지 않음.
- 검증:
  - `python3 -m py_compile` 통과:
    - `isaac_runtime.py`
    - `vla_validation.py`
  - 1차 homefix 검증:
    - output: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oft200_6000_single_orange_homefix_1ep_seed3026.json`
    - 결과: `phase=settling_layout`, `layout_rejections=59`, `home_reset_fallbacks=0`
    - home reset 병목은 해소됐지만 layout validation에서 진행이 늦어짐.
  - 2차 homefix+layoutfix 검증:
    - output: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oft200_6000_single_orange_homefix_layoutfix_1ep_seed3026.json`
    - Isaac log: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/isaac_validation_oft200_6000_single_orange_homefix_layoutfix_1ep_seed3026.log`
    - OFT server log: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oft_server_oft200_6000_single_orange_homefix_layoutfix_8787.log`
    - `status=complete`, `phase=complete`
    - `completed=1`, `successes=0`, `failures=1`
    - `layout_rejections=0`
    - `home_reset_fallbacks=0`
    - policy action records: `90`
    - last `server_request_index=90`
    - 실패 사유: `policy_step_timeout`
    - final `target_lift_m=0.0`
    - final `target_wrist_xy_m=0.3725`
    - final `target_wrist_z_m=0.0599`
- 결론:
  - `request_count=0`/policy 미진입 문제는 색 문제가 아니라 validation setup state machine 문제였음.
  - 1-cube/orange 조건은 이제 rollout까지 정상 진입함.
  - 단, 후처리 없이 `geometry gate off`, `orientation lock off` 조건에서는 1ep도 `policy_step_timeout`으로 실패.
  - 따라서 setup 병목은 해결됐고, 남은 병목은 기존 결론과 동일하게 policy closed-loop 성능임.

## 2026-08-26 17:35 KST - OFT 분석 종료 및 보고서 갱신

- 사용자 요청:
  - real/재학습은 보류.
  - 현재 checkpoint 기준 분석부터 마무리.
  - 결과보고서와 작업기록을 최신 로그 기준으로 정리.
- 기준 모델:
  - `/home/ubuntu/robot_ws/src/openvla/runs/oftplus_h5_proprio_bounded_oft200_9000--6000_chkpt`
- 기준 데이터/환경:
  - `/home/ubuntu/robot_ws/src/doosan-robot2/raw_dataset_oft_200`
  - `/home/ubuntu/robot_ws/tensorflow_datasets_oft_200_a0509`
  - `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806`
  - `/home/ubuntu/robot_ws/world_model_baseline`
- 완료한 분석:
  - raw dataset image/proprio -> model action vs GT action 비교 완료.
    - full corrected output: `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_offline_action_mae_trainval_full_corrected.json`
    - total transitions: `9,847`
    - xyz L2 MAE: `0.00329m`
    - rotation L2 MAE: `0.02913rad`
    - gripper MAE: `0.03509`
    - train/val 격차 작음.
  - Isaac initial frame vs nearest raw initial frame teacher-forced action 비교 완료.
    - output: `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_isaac_frame_teacher_forced.json`
    - raw image XY cosine to nearest raw GT mean: `0.9956`
    - Isaac image XY cosine to nearest raw GT mean: `0.8754`
    - Isaac minus raw xyz L2 mean: `0.00715m`
    - 일부 case는 Isaac frame에서 cosine이 `~0.6`까지 하락.
  - 실패 rollout action/proprio timing 분석 완료.
    - output dir: `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_closed_loop_timing`
    - fixed/random 일반 home은 step 0부터 proprio/nearest raw OOD.
    - 중후반에는 joint3/joint4/joint6 branch/proprio drift와 action norm collapse가 지배적.
    - gripper close는 주로 뒤늦게 올라오며, 이미 XY/Z 정렬이 무너진 뒤라 lift로 이어지지 않음.
  - rotation/branch 분석 완료.
    - output dir: `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_rotation_actions`
    - raw offline rotation 축 상관은 높지만 approach 구간 large-rotation sample은 과소예측.
    - branch jump는 rotation action 단독 원인이라기보다 IK branch 선택, closed-loop target drift, proprio branch mismatch가 함께 작용.
  - raw/Isaac camera crop 비교 완료.
    - output: `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_isaac_raw_image_compare/isaac_raw_initial_frame_comparison.json`
    - contact sheet: `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_isaac_raw_image_compare/isaac_raw_initial_frame_contact_sheet.jpg`
    - mean abs RGB diff: `65.70/255`
    - component centroid abs delta mean: `50.1px x, 45.4px y`
  - 1-cube orange validation setup 경로 수정 후 rollout 진입 확인.
    - output: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oft200_6000_single_orange_homefix_layoutfix_1ep_seed3026.json`
    - result: `0/1`, `policy_step_timeout`
    - final target lift: `0.0m`
    - final target-wrist XY: `0.3725m`
    - mean XY action norm: `0.00271m`, last XY action norm: `0.00038m`
- 최종 판정:
  - GT replay가 통과했으므로 action scale/좌표계/replay 파이프라인은 주원인이 아님.
  - raw teacher-forced action 복원은 양호하므로 checkpoint가 raw action을 전혀 못 배운 상태도 아님.
  - Isaac camera/domain gap은 실제 model action을 바꾸지만, 1-cube/orange 조건에서도 실패하므로 단독 원인은 아님.
  - 후처리 없이 closed-loop로 실행하면 proprio/branch drift와 action collapse가 반복되고 lift가 발생하지 않음.
  - 현재 checkpoint는 real 투입 전 단계가 아니며, 분석상 다음 단계는 데이터 보강/재학습 준비와 sim 재검증임.
- 문서 갱신:
  - `/home/ubuntu/robot_ws/A0509_1-3_WorldModel_실험_파이프라인_결과보고서.md`
  - 9.1장에 1-cube/orange 최신 결과 추가.
  - 12장 최종 판정에 camera/domain gap 및 1-cube/orange 실패 근거 추가.
  - 13장을 “분석 종료 / 재학습 전 입력 계약 / 다음 평가 순서”로 정리.

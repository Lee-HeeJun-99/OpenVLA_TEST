# A0509 VLA Linux Field Bundle 분석

## 개요
- 대상 폴더: `/home/ubuntu/a0509_vla_linux_field_bundle_20260903`
- 성격: A0509 로봇용 OpenVLA/VLA 시뮬레이션 검증 및 현장 시운전용 번들
- 전체 크기: 약 6.2G

## 상위 구성
- `MANIFEST.json`: 번들 설명서와 모델 메타정보
- `isaac_sim/`: Isaac Sim 제어 코드와 검증 실행기
- `validation/`: 장면 자산, URDF, 메시, 로그, 검증 리포트, 비디오 증거
- `environment/`: Ubuntu 22.04 / Python 3.10 기반 오프라인 가상환경

## 문서 파일
- 루트의 일반적인 `README.md`나 `docs/` 디렉터리는 없음
- 확인된 문서성 파일은 `validation/a0509_project/USD/version_1/README_SCALED_85x60.txt` 정도

## 모델/학습 산출물
- `MANIFEST.json`에는 모델 파일명이 명시되어 있음
- OFT 항목: `lora_adapter/adapter_model.safetensors`, `action_head--28560_checkpoint.pt`, `vision_backbone--28560_checkpoint.pt`
- vanilla 항목: `adapter/adapter_model.safetensors`
- 그러나 번들 전체에서 실제 `.safetensors` / `.pt` 가중치 파일은 확인되지 않음
- 즉, 이 번들에는 학습된 모델 weight 자체보다 메타정보와 검증 결과가 들어 있음

## 모델이 무엇을 하는지
- `oft`: `s1_mixed480_oftplus_s1101_b24_e43p2`, step 28560, vision-only, horizon 5, OFT+ FiLM 계열
- `vanilla`: `success_profile_s1_balanced`, step 8130, vision-only, horizon 1, OpenVLA token action 계열
- 둘 다 `MANIFEST.json` 기준으로 closed-loop validation 성공률 1.0으로 기록됨

## 실행 코드
- `isaac_sim/a0509_control_app.py`: A0509 IK/RRT 제어와 레코딩을 담당하는 메인 앱
- `isaac_sim/run_oft_validation.py`: 실행 중인 OFT 서버에 붙어서 검증을 돌리는 스크립트
- `isaac_sim/a0509_control/vla_client.py`: 이미지와 선택적 proprio state를 서버에 보내는 클라이언트

## 시뮬레이션 자산
- `validation/a0509_project/urdf/a0509.urdf`
- `validation/a0509_project/USD/version_1/` 아래의 USD/USDA 파일
- `validation/a0509_project/mesh/` 아래의 시각화/충돌 메시
- `validation/layouts/validation_layouts_seed9406_15.json`: 평가용 레이아웃

## 검증 증거
- `validation/evidence/oft/`와 `validation/evidence/vanilla/`에 checkpoint validation summary와 에피소드 리포트가 있음
- `cube_*`, `open_*` 로그와 PNG 결과 파일이 함께 들어 있음

## 결론
- 이 번들은 학습 코드 저장소라기보다, 학습된 정책을 검증하고 Isaac Sim에서 재현하기 위한 배포용 묶음에 가깝다
- 실제 모델 가중치 파일은 현재 확인된 번들 내부에는 없고, 외부 모델 경로나 서버에서 제공되는 구조로 보인다

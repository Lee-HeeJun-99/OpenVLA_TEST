# A0509 1-3 World Model 실험 파이프라인 결과보고서

작성일: 2026-08-26

## 1. 최종 요약

1-3 데이터 기반 A0509 VLA/World Model 실험의 최종 결론은 다음과 같다.

> GT action replay는 Isaac Sim에서 성공했으므로 데이터 action scale, 좌표계, replay, 기본 IK/제어 파이프라인은 주요 실패 원인에서 제외된다. 반면 정확한 `oft200_6000` checkpoint는 raw dataset image/state에 대해서는 cube 위치에 반응하지만, Isaac closed-loop에서는 자기 action 누적으로 proprio가 학습 분포 밖으로 밀리고 action이 작아지며 grasp/lift에 실패한다. 후처리 없이, 맞춰야 하는 입력/IK 정합성만 적용한 검증에서도 실패가 유지되므로 현재 병목은 validation gain 문제가 아니라 현재 OFT checkpoint의 closed-loop policy 성능과 데이터 분포 한계다.

중요한 해석 기준:

- 모델 output을 바꾸는 후처리는 정식 성능 평가에 사용하지 않는다.
- 허용한 정합성 보정은 학습/runtime 입력 계약을 맞추는 범위로 제한한다.
- 따라서 `geometry gate`, action 방향 보정, gripper timing 차단, `orientation lock` 결과는 성공 근거로 사용하지 않는다.
- 최종 판정은 `gate off`, `orientation lock off`, state/proprio 표현 정합성만 적용한 결과를 기준으로 한다.

## 2. 실험 환경

### 2.1 기준 데이터

- TFDS 데이터셋: `/home/ubuntu/robot_ws/tensorflow_datasets_oft_200_a0509`
- TFDS 버전 경로: `/home/ubuntu/robot_ws/tensorflow_datasets_oft_200_a0509/a0509_sim_cube_pick/1.0.0`
- raw 원본 데이터셋: `/home/ubuntu/robot_ws/src/doosan-robot2/raw_dataset_oft_200`
- dataset key: `a0509_sim_cube_pick`
- train split: 198 episodes, 8875 transitions
- val split: 22 episodes, 972 transitions
- instruction: `Pick up the orange cube.`
- target color: `orange`

Action convention:

- `delta_x_world_m`
- `delta_y_world_m`
- `delta_z_world_m`
- `delta_rotation_vector_x_rad`
- `delta_rotation_vector_y_rad`
- `delta_rotation_vector_z_rad`
- `gripper_closedness_target_0_open_1_closed_continuous`

주의: `/home/ubuntu/robot_ws/raw_dataset_oft`는 100 episode짜리 이전/다른 데이터셋이므로 이번 OFT 모델 분석 기준에서 제외한다.

### 2.2 OFT 모델

- checkpoint: `/home/ubuntu/robot_ws/src/openvla/runs/oftplus_h5_proprio_bounded_oft200_9000--6000_chkpt`
- variant: `oftplus_h5_proprio`
- action chunk size: 5
- action head: L1 regression
- FiLM: enabled
- proprio input: enabled
- bounded gripper: enabled
- continuous gripper: enabled
- center crop: enabled

checkpoint의 `a0509_training_config.json`:

```json
{
  "bounded_gripper": true,
  "gripper_loss_weight": 3.0,
  "use_l1_regression": true,
  "use_diffusion": false,
  "use_film": true,
  "use_proprio": true,
  "action_chunk_size": 5
}
```

checkpoint의 action normalization mask는 `[true, true, true, true, true, true, false]`이다. 즉 position/rotation 6축은 unnormalize되지만 gripper는 bounded action head의 sigmoid 출력 `[0, 1]`을 직접 사용한다.

### 2.3 Isaac Sim 환경

- Isaac Sim package: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806`
- validation entrypoint: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/run_vla_validation.sh`
- GT replay entrypoint: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/run_phase2_gt_replay.sh`

### 2.4 World Model baseline

- workspace: `/home/ubuntu/robot_ws/world_model_baseline`
- baseline dataset: `/home/ubuntu/robot_ws/tensorflow_datasets_oft_200_a0509/a0509_sim_cube_pick/1.0.0`

## 3. Phase별 진행 상태

| Phase | 목적 | 현재 상태 | 판정 |
| --- | --- | --- | --- |
| Phase 1 | 1-3 데이터 수치 검증 | 완료 | 데이터 action 분포 정상 |
| Phase 2 | Isaac Sim GT action replay | 완료 | 통과 |
| Phase 3 | OFT offline/first-action 검증 | 완료 | raw 입력에서는 image sensitivity 확인 |
| Phase 4 | OFT + Isaac closed-loop 검증 | 완료 | 실패 |
| Phase 5 | 실제 A0509 검증 | 보류 | 현재 checkpoint 개선 전 진행 비권장 |
| Phase 6 | 1-step World Model baseline | 완료 | baseline 동작 확인 |
| Phase 7 | Multi-step World Model rollout | 완료 | horizon 증가에 따른 오차 증가 확인 |
| Phase 8 이후 | Isaac/WM/Real 비교, policy eval, planning | 미진행 | OFT policy 개선 후 진행 |

## 4. 데이터 분포 검증

raw 기준 데이터 `/home/ubuntu/robot_ws/src/doosan-robot2/raw_dataset_oft_200`의 train split 198 episode를 확인했다.

### 4.1 Cube 위치 분포

- cube x range: `0.4205 ~ 0.6605 m`
- cube y range: `-0.32242 ~ +0.32342 m`
- y grid bins:
  - `-0.32242`
  - `-0.19325`
  - `-0.06408`
  - `+0.06508`
  - `+0.19425`
  - `+0.32342`

이번 기준 데이터는 fixed joint start + cube 위치 grid 변화 구조다. 따라서 시작 pose 다양성 부족을 주원인으로 보지는 않는다. 이 실험의 목표 자체가 고정 시작 pose에서 cube 위치 변화에 따른 pick behavior 학습이기 때문이다.

### 4.2 GT first-action과 cube 위치 상관

train split 기준:

- `corr(first_action_y, cube_y) = 0.9709`
- `corr(first_action_y, dy_to_cube) = 0.9709`
- `corr(first_action_x, cube_x) = 0.9333`

y bin별 GT first action y 평균:

| cube y | episodes | GT first action y mean |
| ---: | ---: | ---: |
| `-0.32242` | 28 | `-0.008407` |
| `-0.19325` | 36 | `-0.005298` |
| `-0.06408` | 33 | `-0.001566` |
| `+0.06508` | 38 | `+0.002281` |
| `+0.19425` | 35 | `+0.005768` |
| `+0.32342` | 28 | `+0.010016` |

따라서 raw 데이터 자체에는 cube 위치에 따른 action 신호가 존재한다.

### 4.3 GT gripper 분포

- first action gripper: 항상 `0.0`
- first close step:
  - min: 18
  - median: 26
  - max: 29

데이터상 초반에는 open 상태로 접근하고, grasp 구간에서 close가 발생하는 sequence 구조가 명확하다.

### 4.4 Action coverage 주의점

raw dataset 전체에서 `delta_x < 0` row가 없었다. 따라서 validation layout이 wrist 기준 `-X` 이동을 요구하면 현재 모델에는 구조적으로 불리하다.

다만 이 점은 모든 실패의 주원인은 아니다. 기존 fixed/random 실패 중에는 cube가 wrist보다 `+X` 쪽에 있어 dataset coverage 안에 있는 경우도 실패했다.

## 5. Isaac Sim GT Replay 결과

GT action sequence를 Isaac Sim A0509에 그대로 입력한 replay는 통과했다.

대표 결과:

| episode | status | IK successes | IK failures |
| --- | --- | ---: | ---: |
| `episode_000001_commanded_ref.json` | complete | 46 | 0 |
| `episode_000021_commanded_ref.json` | complete | 41 | 0 |
| `episode_000180_commanded_ref.json` | complete | 42 | 0 |

결론:

- dataset action을 Isaac에 넣으면 trajectory가 재현된다.
- action scale, 좌표계, delta action 정의, replay 파이프라인은 큰 문제로 보기 어렵다.
- IK도 GT replay에서 안정적으로 성공했다.

관련 결과:

- `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/phase2_gt_replay/episode_000001_commanded_ref.json`
- `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/phase2_gt_replay/episode_000021_commanded_ref.json`
- `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/phase2_gt_replay/episode_000180_commanded_ref.json`

## 6. OFT Offline / Image Sensitivity 결과

정확한 `oft200_6000` checkpoint로 raw dataset step0 이미지들을 넣어 image sensitivity를 재확인했다.

### 6.1 Fixed proprio sensitivity

- cases: 9
- fixed proprio:
  - `[-3.09236, -0.51718, -1.19633, 0.02577, -1.40699, 0.10139, 0.0]`
- mean predicted action xyz:
  - `[0.00933, -0.00096, -0.02077]`
- std predicted action xyz:
  - `[0.00197, 0.00961, 0.00081]`
- mean XY action norm: `0.01342m`
- dataset GT XY vs predicted XY mean cosine: `0.980`

결과:

- `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_image_sensitivity_fixed_state_step0.json`

### 6.2 Dataset proprio sensitivity

- cases: 9
- mean predicted action xyz:
  - `[0.00732, -0.00143, -0.02179]`
- std predicted action xyz:
  - `[0.00193, 0.01042, 0.00126]`
- mean XY action norm: `0.01244m`
- dataset GT XY vs predicted XY mean cosine: `0.996`

결과:

- `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_image_sensitivity_dataset_state_step0.json`

### 6.3 Offline action MAE

정확한 `oft200_6000` checkpoint를 raw train/val image + proprio에 teacher-forced로 넣고 GT action과 비교했다. 각 episode 마지막 hold row는 `observation_t_to_observation_t_plus_1` transition이 아니므로 제외했고, split은 raw dataset의 `metadata/splits.json`을 기준으로 재집계했다.

실행 스크립트:

- `/home/ubuntu/robot_ws/world_model_baseline/run_oft_offline_action_mae.py`

결과:

- full corrected: `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_offline_action_mae_trainval_full_corrected.json`
- smoke 60/sample split: `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_offline_action_mae_smoke_trainval60.json`

전체 9,847 transitions 기준 MAE:

| axis | MAE |
| --- | ---: |
| x | `0.000825 m` |
| y | `0.001372 m` |
| z | `0.002297 m` |
| rot x | `0.01455 rad` |
| rot y | `0.02072 rad` |
| rot z | `0.00882 rad` |
| gripper | `0.03509` |

split별 요약:

| split | count | xyz L2 MAE | rotation L2 MAE | gripper MAE | XY cosine |
| --- | ---: | ---: | ---: | ---: | ---: |
| train | 8,875 | `0.00330 m` | `0.02913 rad` | `0.03520` | `0.9616` |
| val | 972 | `0.00317 m` | `0.02915 rad` | `0.03407` | `0.9514` |

phase별 요약:

| phase | count | xyz L2 MAE | rotation L2 MAE | gripper MAE | 비고 |
| --- | ---: | ---: | ---: | ---: | --- |
| approach step 0~30 | 5,720 | `0.00363 m` | `0.04435 rad` | `0.03088` | rotation 오차가 가장 큼 |
| close 직전 5 step | 1,100 | `0.00202 m` | `0.00766 rad` | `0.09174` | gripper가 먼저 올라가는 경향 |
| close 이후 | 3,027 | `0.00310 m` | `0.00819 rad` | `0.02245` | z signed error `-0.00239 m` |

해석:

- 정확한 `oft200_6000` 모델은 raw dataset step0 이미지/상태에 대해 cube 위치 변화에 반응한다.
- full offline MAE에서도 train/val raw 분포에 대해서는 position action 복원이 mm 단위로 작고, train/val 간 격차도 작다.
- 따라서 checkpoint가 raw train/val action을 전혀 못 복원하는 상태는 아니다.
- 다만 접근 구간 rotation L2 MAE가 `0.04435 rad`로 크고, close 직전 gripper MAE가 `0.09174`로 커서 rotation branch jump와 gripper timing 문제는 별도 원인 분리 대상이다.
- 따라서 “모델이 이미지를 전혀 보지 않는다”는 결론은 폐기한다.
- 문제의 큰 축은 raw/offline action 복원 실패라기보다 Isaac closed-loop rollout에서 발생하는 drift, proprio OOD, camera/domain gap 쪽이다.

## 7. OFT Closed-loop 검증 결과

### 7.1 기존 fixed L/C/R closed-loop

- result: `0/3`
- failure reason: 모두 `policy_step_timeout`
- IK: 전부 `90/90` 성공
- cube lift: 전부 `0.00000 m`

episode별 요약:

| episode | cube y | reason | steps | target lift | max gripper | first gripper close step |
| ---: | ---: | --- | ---: | ---: | ---: | ---: |
| 1 | `-0.25` | `policy_step_timeout` | 90 | `0.00000` | `0.9805` | 79 |
| 2 | `0.00` | `policy_step_timeout` | 90 | `0.00000` | `0.9883` | 60 |
| 3 | `+0.25` | `policy_step_timeout` | 90 | `0.00000` | `0.9805` | 77 |

결과:

- `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oftplus_h5_proprio_bounded_oft200_6000_fixed_lcr_closed_loop_after_gtfix_rerun_seed3026.json`

### 7.2 기존 random 5 closed-loop

- result: `0/5`
- failure reason: 모두 `policy_step_timeout`
- IK: 전부 `90/90` 성공
- cube lift: 사실상 `0 m`

episode별 요약:

| episode | cube y | reason | steps | target lift | max gripper | first gripper close step |
| ---: | ---: | --- | ---: | ---: | ---: | ---: |
| 1 | `-0.1765` | `policy_step_timeout` | 90 | `-0.00000` | `0.9844` | 52 |
| 2 | `+0.0523` | `policy_step_timeout` | 90 | `0.00000` | `0.9844` | 54 |
| 3 | `-0.1393` | `policy_step_timeout` | 90 | `0.00000` | `0.9844` | 57 |
| 4 | `-0.0097` | `policy_step_timeout` | 90 | `0.00012` | `0.3984` | none |
| 5 | `-0.0717` | `policy_step_timeout` | 90 | `0.00000` | `0.9844` | 54 |

결과:

- `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oftplus_h5_proprio_bounded_oft200_6000_random5_closed_loop_after_gtfix_rerun_seed4026.json`

### 7.3 Closed-loop failure timing

각 validation action마다 checkpoint proprio q01/q99로 정규화한 7D proprio 기준 nearest raw state를 붙이고, step별 target distance, action norm, rotation norm, gripper, proprio OOD를 분석했다.

분석 스크립트:

- `/home/ubuntu/robot_ws/world_model_baseline/analyze_oft_closed_loop_timing.py`

결과 디렉터리:

- `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_closed_loop_timing`

생성 결과:

- summary: `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_closed_loop_timing/closed_loop_timing_summary.json`
- fixed L/C/R plot: `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_closed_loop_timing/oftplus_h5_proprio_bounded_oft200_6000_fixed_lcr_closed_loop_after_gtfix_rerun_seed3026_timing.png`
- random5 plot: `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_closed_loop_timing/oftplus_h5_proprio_bounded_oft200_6000_random5_closed_loop_after_gtfix_rerun_seed4026_timing.png`
- training-home plot: `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_closed_loop_timing/oftplus_h5_proprio_bounded_oft200_6000_training_home_90step_1ep_seed3026_timing.png`

fixed L/C/R summary:

| metric | value |
| --- | ---: |
| rows | `270` |
| IK success fraction | `1.000` |
| target XY start mean | `0.3027 m` |
| target XY end mean | `0.1796 m` |
| mean XY action norm | `0.00240 m` |
| mean XY closing | `0.00103 m` |
| away fraction | `0.367` |
| XY dead while far fraction | `0.778` |
| rotation norm mean / p95 | `0.0690 / 0.3487 rad` |
| proprio OOD any fraction | `1.000` |
| nearest raw normalized distance mean | `19.29` |

random5 summary:

| metric | value |
| --- | ---: |
| rows | `450` |
| IK success fraction | `1.000` |
| target XY start mean | `0.2572 m` |
| target XY end mean | `0.1123 m` |
| mean XY action norm | `0.00233 m` |
| mean XY closing | `0.00129 m` |
| away fraction | `0.387` |
| XY dead while far fraction | `0.671` |
| rotation norm mean / p95 | `0.0691 / 0.3516 rad` |
| proprio OOD any fraction | `1.000` |
| nearest raw normalized distance mean | `19.58` |

training-home 1ep summary:

| metric | value |
| --- | ---: |
| rows | `90` |
| IK success fraction | `1.000` |
| target XY start -> end | `0.0837 -> 0.1814 m` |
| mean XY action norm | `0.00066 m` |
| mean XY closing | `-0.00043 m` |
| away fraction | `0.956` |
| XY dead while far fraction | `0.856` |
| proprio OOD any fraction | `0.989` |
| nearest raw normalized distance mean | `6.56` |

failure 순서:

- fixed/random 일반 home:
  - step 0부터 proprio OOD 및 nearest raw OOD.
  - step 0부터 rotation norm이 `~0.35rad`까지 튄다.
  - XY action은 초반 이후 `~0.002m` 수준으로 작아지고, target까지 아직 먼데도 dead step이 다수 발생한다.
  - away XY는 episode별 step `5~33`에서 시작한다.
  - gripper close는 step `52~79`로 늦게 올라오지만, 이미 XY/Z 정렬이 무너진 뒤라 lift로 이어지지 않는다.
- 정확한 `oft200_6000` training-home:
  - step 0 initial proprio는 학습 branch에 맞지만, 첫 action부터 XY가 target 반대 방향이다.
  - step 1부터 proprio OOD, step 3부터 nearest raw OOD가 발생한다.
  - target XY distance가 `0.0837m`에서 `0.1814m`로 악화된다.
  - gripper는 끝까지 close threshold에 도달하지 않는다.

해석:

- 일반 home 실패는 먼저 proprio OOD/rotation branch mismatch가 터지고, 그 뒤 XY action collapse와 late gripper가 이어지는 패턴이다.
- training-home에서도 initial proprio mismatch를 없애면 rotation branch jump는 줄지만, 첫 action부터 XY 방향성이 틀리고 곧 closed-loop state가 raw nearest distribution 밖으로 나간다.
- 따라서 현재 실패 순서는 “gripper가 먼저 닫혀서 실패”가 아니라, `proprio/rotation branch 또는 camera-domain 입력에서 policy가 잘못된 XY/rotation을 내고 -> closed-loop state OOD -> action norm collapse -> gripper는 뒤늦게 올라옴`에 가깝다.

### 7.4 Rotation action 분석

offline predicted rotation과 GT rotation을 비교하고, closed-loop에서 rotation action과 joint4/joint6 branch jump timing을 같이 확인했다.

분석 스크립트:

- `/home/ubuntu/robot_ws/world_model_baseline/analyze_oft_rotation_actions.py`

결과 디렉터리:

- `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_rotation_actions`

요약 파일:

- `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_rotation_actions/rotation_action_summary.json`

raw rotation action 분포:

- rotation norm mean: `0.1480 rad`
- rotation norm q95/q99/max: `0.4121 / 0.6227 / 2.1354 rad`

offline rotation 복원:

| scope | rot x MAE | rot y MAE | rot z MAE | error norm mean | error norm p95 |
| --- | ---: | ---: | ---: | ---: | ---: |
| all | `0.01455` | `0.02072` | `0.00882` | `0.02913` | `0.16694` |
| approach 0~30 | `0.02337` | `0.03076` | `0.01403` | `0.04435` | `0.30157` |
| close 직전 5 step | `0.00433` | `0.00579` | `0.00127` | `0.00766` | `0.00937` |
| close 이후 | `0.00161` | `0.00716` | `0.00171` | `0.00819` | `0.01550` |

axis별 GT/pred correlation:

- all: rot x `0.907`, rot y `0.921`, rot z `0.947`
- approach 0~30: rot x `0.855`, rot y `0.915`, rot z `0.931`

closed-loop rotation / branch timing:

| run | rot norm mean | rot norm p95 | joint4 OOD | joint6 OOD | joint4 branch jump | joint6 branch jump |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| fixed L/C/R | `0.0690` | `0.3487` | `0.978` | `0.133` | `0.933` | `0.059` |
| random5 | `0.0691` | `0.3516` | `0.978` | `0.129` | `0.933` | `0.058` |
| training-home | `0.0098` | `0.0108` | `0.989` | `0.433` | `0.356` | `0.344` |

event timing:

- fixed/random 일반 home:
  - joint6 branch는 step 0부터 이미 `~pi` branch에 있다.
  - joint4 OOD는 모든 episode에서 step 2.
  - joint4 branch jump는 모든 episode에서 step 6.
  - rotation action norm은 raw q99 `0.6227rad`보다 크지는 않지만, 초반 p95가 `~0.35rad`로 크고 branch 변화와 같은 초반 구간에 발생한다.
- training-home:
  - rotation action은 평균 `0.0098rad`, p95 `0.0108rad`로 작다.
  - 그런데 joint4 OOD는 step 1, joint4/joint6 branch jump는 step 58/59에 발생한다.
  - 따라서 branch jump가 “큰 rotation action 하나”만으로 생긴다고 보기는 어렵고, IK branch 선택/closed-loop target drift/proprio branch가 같이 얽혀 있다.

해석:

- offline raw에서는 rotation 축 상관이 높으므로 모델이 rotation을 완전히 못 배우는 상태는 아니다.
- 하지만 approach 구간의 큰 GT rotation outlier는 모델이 과소예측한다. top error는 approach step에서 GT rotation norm이 매우 큰 sample들이다.
- closed-loop 일반 home의 joint6 branch 문제는 rotation action 이전부터 시작 pose/proprio branch mismatch로 존재한다.
- joint4 branch jump는 초반 rotation target과 IK branch 선택의 영향을 받을 수 있지만, training-home에서도 작은 rotation action으로 나중에 branch jump가 생기므로 rotation action만 단독 원인으로 단정하면 안 된다.
- 다음 확인은 camera/domain gap이다. raw/offline은 맞는데 Isaac closed-loop에서 첫 XY 방향부터 틀어지는지 확인하려면 raw image와 Isaac validation initial frame을 같은 crop/resize로 비교해야 한다.

### 7.5 Isaac camera vs raw image 비교

validation initial frame마다 target XY가 가장 가까운 raw episode step0을 찾고, 양쪽 이미지를 같은 center-crop + 224 resize로 비교했다.

분석 스크립트:

- `/home/ubuntu/robot_ws/world_model_baseline/compare_isaac_raw_initial_frames.py`

결과:

- report: `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_isaac_raw_image_compare/isaac_raw_initial_frame_comparison.json`
- contact sheet: `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_isaac_raw_image_compare/isaac_raw_initial_frame_contact_sheet.jpg`

수치 요약:

| metric | value |
| --- | ---: |
| compared pairs | `9` |
| mean abs RGB diff | `65.70 / 255` |
| raw orange component pixels mean | `139.9 px` |
| Isaac red component pixels mean | `81.4 px` |
| Isaac/raw component area ratio mean | `0.60` |
| centroid abs delta mean | `50.1px x, 45.4px y` |
| nearest raw target XY distance mean | `0.0489m` |

정성 확인:

- raw는 실제 ZED eye-in-hand 이미지이고, 작은 orange cube가 실제 테이블 위에 있으며 gripper/카메라 노출/테이블 질감이 학습 데이터 그대로다.
- Isaac은 렌더링 이미지이고, 여러 cube가 동시에 보이며 target은 `red`로 기록된다. instruction은 `Pick up the orange cube.`라 raw 학습 target color와 validation target color/appearance가 맞지 않는다.
- Isaac frame에는 강한 로봇 그림자, 렌더링 테이블 색, 다른 gripper/robot appearance가 크게 보인다.
- 같은 center crop 이후에도 cube component 위치가 평균 `~50px` 수준으로 달라진다.
- Isaac red cube segmentation은 여러 렌더링 색/로봇 부품과 섞일 수 있어, 수치보다 contact sheet를 우선 해석해야 한다.

해석:

- offline raw에서는 cube 위치에 반응하지만 Isaac closed-loop 첫 action이 틀어지는 현상은 camera/domain gap으로도 충분히 설명 가능하다.
- 특히 validation scene은 raw의 “단일 orange cube pick”보다 다중 cube, 다른 target color, 렌더링 조명/그림자/배경 차이가 크다.
- 다음 실험은 Isaac initial frame을 OFT 서버에 teacher-forced로 넣고, 같은 target XY raw nearest sample의 model action과 직접 비교하는 방식이 적합하다.

## 8. Closed-loop 분포 분석

checkpoint proprio q01/q99:

- q01: `[-3.793, -0.919, -1.854, -0.075, -1.981, -0.597, 0.0]`
- q99: `[-2.483, 0.052, -0.566, 0.086, -1.000, 0.713, 1.0]`

기존 validation에서는 closed-loop 중후반에 joint3/joint4가 학습 분포 밖으로 크게 벗어났다. 특히 joint4는 raw mean이 `~0rad` 근처인데 validation 중후반에는 `3.x rad` branch로 갔다.

분석 결과:

- raw dataset phase별 OOD any는 대체로 `7~17%` 수준
- 기존 validation OOD any는 `100%`
- 중후반 validation은 joint3와 joint4가 q01/q99 밖인 비율이 매우 높음
- action magnitude는 중후반에 `0.0003~0.0005m` 수준으로 collapse되는 패턴이 확인됨
- gripper close는 올라가지만 XY/Z 정렬과 lift로 이어지지 않음

관련 결과:

- `/home/ubuntu/robot_ws/world_model_baseline/analyze_oft_closed_loop_distribution.py`
- `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_fixed_lcr_closed_loop_distribution.json`
- `/home/ubuntu/robot_ws/world_model_baseline/oft200_6000_random5_closed_loop_distribution.json`

## 9. 맞춰야 하는 것만 맞춘 최종 확인

사용자 지적에 따라 모델 output 후처리는 제외하고, 학습/runtime 계약상 맞춰야 하는 항목만 적용해 재검증했다.

적용한 정합성:

- validation home pose를 raw dataset first-step 평균 branch로 설정
- OFT 입력 proprio를 checkpoint `proprio` q01/q99 기준 2π branch로 wrapping
- IK에 현재 관절값 warm-start 제공

적용하지 않은 것:

- geometry gate
- action 방향 보정
- action scale/gain 증폭
- gripper close 차단
- orientation lock

실행 조건:

- checkpoint: `/home/ubuntu/robot_ws/src/openvla/runs/oftplus_h5_proprio_bounded_oft200_9000--6000_chkpt`
- output: `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oft200_6000_state_ikmatch_only_90step_1ep_seed3026.json`
- geometry gate: off
- orientation lock: off
- proprio offset: `[0, 0, 0, 0, 0, 0]`
- home pose: raw dataset first-step 평균 branch

결과:

- 실패, `policy_step_timeout`
- policy steps: 90
- IK success: `90/90`
- final target-wrist XY: `0.3130m`
- final target-wrist Z: `0.3927m`
- target lift: `0.0m`
- mean XY action norm: `0.00291m`
- close mean: `0.589`

proprio:

```text
first raw proprio:
[-3.1879, -0.0210, -0.7156, 0.0218, -1.9810, 0.0000, 0.0]

last raw proprio:
[-3.6608, -0.2145, -1.7916, 3.1241, -0.6340, 4.3315, 0.9802]

last model proprio after wrapping:
[-3.6608, -0.2145, -1.7916, 3.1241, -0.6340, -1.9517, 0.9802]
```

OOD 비율:

- proprio OOD any: `0.989`
- joint1: `0.056`
- joint2: `0.000`
- joint3: `0.789`
- joint4: `0.989`
- joint5: `0.289`
- joint6: `0.967`
- gripper: `0.000`

해석:

- 시작 proprio는 raw first-step branch와 맞다.
- 그러나 모델 action을 그대로 실행하면 closed-loop 중 joint4/joint6 branch가 다시 raw 학습 분포 밖으로 간다.
- 후처리 없이 보면 실패가 유지된다.
- 따라서 현재 실패는 단순 validation 입력 표현 mismatch만으로 해결되지 않는다.
- 현재 `oft200_6000` checkpoint는 closed-loop policy로 사용하기에 부족하다고 판단한다.

## 10. 후처리 실험의 위치

중간에 diagnostic 목적으로 geometry gate와 orientation lock을 실험했다.

해당 실험의 목적은 원인 분리이며, 모델 성능 평가에는 사용하지 않는다.

- geometry gate:
  - target 반대 XY action 보정, descent 제한, gripper close 차단을 수행
  - 일부 거리 감소는 만들 수 있으나 planner reachability를 깨거나 평가를 왜곡함
- orientation lock:
  - wrist orientation 누적으로 joint branch가 튀는 현상을 줄임
  - 하지만 모델 rotation action을 무시하는 후처리이므로 정식 성공 근거로 사용할 수 없음

최종 기준은 `gate off`, `orientation lock off`다.

## 11. World Model Baseline 결과

World Model baseline은 `/home/ubuntu/robot_ws/world_model_baseline`에서 기본 검증이 완료되었다.

### 11.1 Linear baseline

- dataset: `/home/ubuntu/robot_ws/tensorflow_datasets_oft_200_a0509/a0509_sim_cube_pick/1.0.0`
- image size: 32
- train transitions: 8677
- val transitions: 950
- state MAE: `0.0051785`
- persistence image MSE: `0.0014910`
- linear residual image MSE: `0.0012380`

결과:

- `/home/ubuntu/robot_ws/world_model_baseline/output_tfds_full_32/report.json`

### 11.2 CNN baseline

- dataset: `/home/ubuntu/robot_ws/tensorflow_datasets_oft_200_a0509/a0509_sim_cube_pick/1.0.0`
- image size: 64
- epochs: 15
- batch size: 128
- device: CUDA
- train transitions: 8677
- val transitions: 950
- state MAE: `0.0046026`
- persistence image MSE: `0.0023600`
- CNN image MSE: `0.0008522`
- persistence image MAE: `0.0152418`
- CNN image MAE: `0.0098730`

결과:

- `/home/ubuntu/robot_ws/world_model_baseline/output_cnn_full_64_gpu/report.json`
- `/home/ubuntu/robot_ws/world_model_baseline/output_cnn_full_64_gpu/cnn_world_model.pt`

### 11.3 Multi-step rollout

- split: val
- episodes: 22
- requested horizon: 10

| horizon | image MSE | image MAE | state MAE |
| ---: | ---: | ---: | ---: |
| 1 | `0.00004697` | `0.0026243` | `0.0047034` |
| 5 | `0.0026346` | `0.0248554` | `0.0161665` |
| 10 | `0.0086322` | `0.0551577` | `0.0281185` |

결과:

- `/home/ubuntu/robot_ws/world_model_baseline/output_cnn_rollout_val_h10/rollout_report.json`
- `/home/ubuntu/robot_ws/world_model_baseline/output_cnn_rollout_val_h10/samples/`

판정:

- 1-step prediction은 baseline으로 동작한다.
- multi-step rollout은 horizon이 길어질수록 오차가 증가한다.
- 현재 OFT policy가 closed-loop에서 실패하므로, World Model 기반 policy evaluation/planning으로 넘어가기 전에 OFT policy 자체 개선이 선행되어야 한다.

## 12. 최종 판정

현재까지의 실험 결과로는 `oft200_6000` checkpoint의 closed-loop policy 문제가 핵심이다.

근거:

- GT replay 통과: 데이터 action, 좌표계, Isaac replay 파이프라인 정상
- raw image sensitivity 통과: 정확한 모델은 raw step0 이미지/상태에서는 cube 위치 변화에 반응
- OFT closed-loop 실패: fixed L/C/R `0/3`, random `0/5`
- state/IK 정합성만 적용한 최종 확인도 실패
- IK는 대부분 성공하므로 단순 IK 실패가 아님
- target cube lift는 발생하지 않음
- closed-loop 중 proprio가 학습 q01/q99 밖으로 크게 벗어남
- action magnitude가 중후반 작아지고, gripper close가 올라가도 grasp/lift로 이어지지 않음

최종 결론:

> 현재 데이터와 Isaac replay 파이프라인은 GT trajectory를 재현할 수 있다. 그러나 현재 `oft200_6000` OFT checkpoint는 자기 action을 closed-loop로 실행할 때 학습 분포 안에서 안정적으로 접근, 정렬, grasp, lift를 유지하지 못한다. 후처리 없이 보면 실패가 유지되므로 현재 병목은 모델/데이터의 closed-loop 일반화 성능이다.

## 13. 권장 다음 작업

### 13.1 현재 checkpoint로 추가 튜닝하지 않을 것

timeout 증가, gain 조정, action 방향 보정, gripper gate, orientation lock은 정식 해결책이 아니다. 진단에는 쓸 수 있지만 성공률 보고에는 포함하지 않는다.

### 13.2 Offline rollout/action diagnostic

다음 항목을 먼저 확인한다.

- train/val episode별 predicted action chunk vs GT action chunk
- step별 action MAE와 sign error
- cube x/y bin별 action MAE
- grasp 구간 gripper MAE
- close timing 예측 step과 GT close step 비교
- rotation action 누적이 joint4/joint6 branch jump를 유발하는지 정량화
- Isaac validation frame과 raw dataset frame의 crop/color/camera 차이 비교

### 13.3 데이터/재학습 방향

- 현재 fixed start 구조는 유지 가능
- 단, closed-loop drift 대응용 corrective/recovery state를 추가하는 것이 필요
- rotation action이 불안정하면 wrist orientation 고정 데이터 또는 rotation loss/representation 재검토
- 실제 runtime에서 읽는 joint 표현 그대로 저장하고 추론에도 같은 표현 사용
- validation layout은 train coverage 안/밖을 분리해서 보고
- `-X` 접근이 필요한 layout까지 목표로 하면 양방향 X action coverage를 추가 수집

### 13.4 다음 평가 순서

1. 현재 checkpoint offline action diagnostic
2. 문제 구간 기준 데이터 보강 또는 재학습
3. raw/val image sensitivity 재확인
4. Isaac fixed L/C/R first-action 재검증
5. Isaac fixed L/C/R closed-loop 재검증
6. random closed-loop 재검증
7. World Model에 policy action chunk를 넣은 rollout 비교
8. Isaac vs World Model vs Real trajectory 비교

## 14. 주요 산출물 경로

최종 OFT 검증:

- `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oft200_6000_state_ikmatch_only_90step_1ep_seed3026.json`

Diagnostic 전용 후처리 검증:

- `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oft200_6000_proprio_branchfix_lockrot_90step_1ep_seed3026.json`

기존 OFT validation:

- `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oftplus_h5_proprio_bounded_oft200_6000_fixed_lcr_first_action_after_gtfix_rerun_seed3026.json`
- `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oftplus_h5_proprio_bounded_oft200_6000_fixed_lcr_closed_loop_after_gtfix_rerun_seed3026.json`
- `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/vla_validation/oftplus_h5_proprio_bounded_oft200_6000_random5_closed_loop_after_gtfix_rerun_seed4026.json`

GT replay:

- `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/phase2_gt_replay/episode_000001_commanded_ref.json`
- `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/phase2_gt_replay/episode_000021_commanded_ref.json`
- `/home/ubuntu/A0509_IsaacSim_Ubuntu_20260806/output/phase2_gt_replay/episode_000180_commanded_ref.json`

Model/checkpoint:

- `/home/ubuntu/robot_ws/src/openvla/runs/oftplus_h5_proprio_bounded_oft200_9000--6000_chkpt`

Dataset:

- `/home/ubuntu/robot_ws/tensorflow_datasets_oft_200_a0509`
- `/home/ubuntu/robot_ws/src/doosan-robot2/raw_dataset_oft_200`

World Model:

- `/home/ubuntu/robot_ws/world_model_baseline/output_tfds_full_32/report.json`
- `/home/ubuntu/robot_ws/world_model_baseline/output_cnn_full_64_gpu/report.json`
- `/home/ubuntu/robot_ws/world_model_baseline/output_cnn_rollout_val_h10/rollout_report.json`
